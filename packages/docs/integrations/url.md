# Moved

The URL codec documentation was split into one page per package:

- [@rapiq/codec-url-simple](/packages/codec-url-simple) — encoding, decoding, the round-trip guarantee, parameter names
- [@rapiq/codec-url-expression](/packages/codec-url-expression) — the expression wire dialect
- [@rapiq/codec-url](/packages/codec-url) — the codec registry (in-band dialect dispatch)

The narrative introduction lives at [Queries over the Wire](/guide/wire).
