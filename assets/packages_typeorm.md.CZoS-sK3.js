import{_ as e,o as i,c as a,a2 as t}from"./chunks/framework.CpXckY1Z.js";const k=JSON.parse('{"title":"@rapiq/typeorm","description":"","frontmatter":{},"headers":[],"relativePath":"packages/typeorm.md","filePath":"packages/typeorm.md","lastUpdated":1784536945000}'),n={name:"packages/typeorm.md"};function l(p,s,d,r,h,o){return i(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="rapiq-typeorm" tabindex="-1">@rapiq/typeorm <a class="header-anchor" href="#rapiq-typeorm" aria-label="Permalink to &quot;@rapiq/typeorm&quot;">​</a></h1><p>Applies a parsed <a href="/guide/query-ast.html"><code>Query</code></a> directly to a TypeORM <code>SelectQueryBuilder</code> — filters become parameterized <code>WHERE</code> conditions, relations become joins, fields/sort/pagination map to <code>select</code>/<code>orderBy</code>/<code>take</code>+<code>skip</code>.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/core</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/sql</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/typeorm</span></span></code></pre></div><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { TypeormAdapter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/typeorm&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> queryBuilder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dataSource.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getRepository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(User).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createQueryBuilder</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TypeormAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    queryBuilder,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    relations: { joinAndSelect: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pagination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">entities</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">total</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> queryBuilder.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getManyAndCount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>The <code>queryBuilder</code> (the builder to write into) is bound at construction; <code>execute(query)</code> then walks the parsed <code>Query</code>, collects the state into its sub-adapters, and applies it to that builder — returning the applied pagination (e.g. for the response <code>meta</code> block).</p><p>Builder state you set before <code>execute</code> is preserved: <code>WHERE</code> conditions stay (rapiq appends its filter tree with <code>AND</code>, and binds its parameters under a private namespace so caller bindings are never rebound), and a query that carries no sorts or pagination leaves a caller-owned <code>ORDER BY</code> / <code>take</code> / <code>skip</code> untouched. An application-owned tenant or authorization predicate therefore remains the baseline even when the client sends no filters. When the client <em>does</em> send sorts, fields or pagination, those replace the corresponding builder state — that is the request&#39;s job.</p><p>Construct the adapter <strong>per request</strong>, just like the <code>SelectQueryBuilder</code> you hand it — it holds per-call state. The shareable, long-lived part is your config, which you spread into the per-request options:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// module scope — the reusable config</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> config</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { relations: { joinAndSelect: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } };</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// per request — add the request&#39;s builder as \`queryBuilder\`</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TypeormAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config, queryBuilder }).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span></code></pre></div><p>By default each call clears the adapter&#39;s own accumulated state. The bound <code>queryBuilder</code>, however, is mutated in place — joins and the selected projection are not rolled back — so build a fresh adapter (with a fresh builder) per query rather than re-running one onto an already-applied builder. Pass <code>{ clear: false }</code> as the second argument to accumulate several queries&#39; conditions onto the same builder, and <code>{ visitor }</code> to forward options to the underlying visitors:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, { clear: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre></div><h2 id="options" tabindex="-1">Options <a class="header-anchor" href="#options" aria-label="Permalink to &quot;Options&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TypeormAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    relations: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        joinAndSelect: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        joinType: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;left&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        onJoin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">alias</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">queryBuilder</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            queryBuilder.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addGroupBy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">alias</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}.id\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Option</th><th>Description</th></tr></thead><tbody><tr><td><code>relations.joinAndSelect</code></td><td>Join <strong>and select</strong> (hydrate the related entities) instead of joining for filtering/sorting only.</td></tr><tr><td><code>relations.joinType</code></td><td><code>&#39;left&#39;</code> (default) or <code>&#39;inner&#39;</code>. Left joins keep records whose relation is absent.</td></tr><tr><td><code>relations.onJoin</code></td><td>Invoked as <code>(path, alias, queryBuilder)</code> for every join the adapter applies — e.g. to <code>addGroupBy</code> per join when the root query is grouped. Skipped (pre-existing) joins don&#39;t trigger it.</td></tr><tr><td><code>relations.relationAlias</code></td><td>Derive the join alias for a relation path (default: collision-free length-prefixed segments, e.g. <code>role.realm</code> → <code>r4_role_5_realm</code>). Filter/sort/field references resolve against the same derivation.</td></tr></tbody></table><p>Relations are validated against the entity metadata of the attached query builder — a requested relation that doesn&#39;t exist on the entity is ignored. Joins are applied idempotently: relations already joined on the query builder (by the adapter or by your own code, matched by alias) are skipped, so applying a query twice does not duplicate joins.</p><div class="warning custom-block"><p class="custom-block-title">Alias convention</p><p>The exported <code>buildRelationAlias(path)</code> helper length-prefixes every segment: <code>realm</code> becomes <code>r5_realm</code>, and <code>role.realm</code> becomes <code>r4_role_5_realm</code>. This remains distinct even from a relation literally named <code>role_realm</code>. Fields, filters, sorts and joins all use the same derivation. Pre-existing joins are matched by that alias; use the helper for joins you apply yourself, or inject one convention via <code>relations.relationAlias</code>. Keep a custom derivation collision-free and within your database&#39;s identifier length limit.</p></div><h2 id="dialect-detection" tabindex="-1">Dialect detection <a class="header-anchor" href="#dialect-detection" aria-label="Permalink to &quot;Dialect detection&quot;">​</a></h2><p>The adapter resolves the SQL dialect from the attached query builder&#39;s connection type (<code>postgres</code>, <code>mysql</code>/<code>mariadb</code>, <code>sqlite</code>/<code>better-sqlite3</code>, <code>mssql</code>, <code>oracle</code>, …). Field escaping is delegated to the query builder itself; regex conditions use the matching <a href="/packages/sql.html#dialects">dialect preset</a> — on regex-less dialects (SQLite, SQL Server) the <code>contains</code> / <code>startsWith</code> / <code>endsWith</code> operators fall back to <code>LIKE</code>, and the <code>regex</code> operator throws a typed <code>AdapterError</code>. When the connection type has no matching preset, the postgres preset is the documented last-resort default.</p><h2 id="case-folding-column-types" tabindex="-1">Case folding &amp; column types <a class="header-anchor" href="#case-folding-column-types" aria-label="Permalink to &quot;Case folding &amp; column types&quot;">​</a></h2><p><a href="/guide/filters.html#case-sensitivity">Case-insensitive string equality</a> folds through <code>lower()</code> on case-sensitive dialects. The adapter resolves each filtered field against the entity metadata (relation paths included) and folds <strong>only string-typed columns</strong> — filtering an <code>int</code> column with an untyped wire string (<code>filter[age]=18</code>) renders a plain <code>=</code> instead of a <code>lower(...)</code> type error, and non-string columns never pay the folding cost. Unresolvable fields keep the folding default; opt fields out explicitly via <code>execute(query, { visitor: { caseSensitive: [...] } })</code>.</p><h2 id="embedded-columns" tabindex="-1">Embedded columns <a class="header-anchor" href="#embedded-columns" aria-label="Permalink to &quot;Embedded columns&quot;">​</a></h2><p>Dotted field paths resolve against the entity metadata segment by segment — only real relations join. A path into an <a href="https://typeorm.io/docs/entity/embedded-entities/" target="_blank" rel="noreferrer">embedded entity</a> (<code>@Column(() =&gt; Profile)</code>), e.g. <code>profile.firstName</code>, is dotted without anything to join: it renders against its parent alias with the embedded column&#39;s database name (<code>&quot;user&quot;.&quot;profileFirstname&quot;</code>) instead of producing a bogus <code>LEFT JOIN</code>. This applies uniformly to filters, sort and field selection, and composes with relations — <code>role.profile.firstName</code> joins only <code>role</code> and resolves the embedded remainder against that join&#39;s alias.</p><h2 id="deriving-schemas-from-entities" tabindex="-1">Deriving schemas from entities <a class="header-anchor" href="#deriving-schemas-from-entities" aria-label="Permalink to &quot;Deriving schemas from entities&quot;">​</a></h2><p>Instead of hand-maintaining a <a href="/guide/schemas.html"><code>Schema</code></a> per resource, derive it from the TypeORM entity metadata. <code>defineSchemaRegistryWithDataSource</code> walks all entities of a data source and returns a populated <code>SchemaRegistry</code> — one schema per entity, cross-linked automatically:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineSchemaRegistryWithDataSource } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/typeorm&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> registry</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineSchemaRegistryWithDataSource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dataSource, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    schemas: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        user: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            filters: { allowed: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;inherit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            sort: { allowed: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;inherit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Every schema gets its <strong>structure</strong> derived unconditionally: the schema name (lower-camel entity name, <code>RoleDetail</code> → <code>roleDetail</code>), the allowed relations, and the <code>schemaMapping</code> linking each relation to its target entity&#39;s schema — so nested paths like <code>role.detail</code> resolve across the registry without any manual wiring.</p><p>Column-based <strong>allow-lists</strong> are opt-in per parameter: <code>allowed: &#39;inherit&#39;</code> expands to the entity&#39;s column property paths. Hidden columns (<code>select: false</code>) and virtual join columns are always excluded; explicitly declared FK columns (e.g. <code>realmId</code>) are included. Any explicit option wins over its derived counterpart:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineSchemaWithEntity } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/typeorm&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> schema</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineSchemaWithEntity</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(User, dataSource, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    strict: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    fields: { allowed: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;inherit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: { allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// explicit list, nothing derived</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sort: { default: { id: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;DESC&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>defineSchemaWithEntity</code> also accepts an <code>EntityMetadata</code> directly (<code>defineSchemaWithEntity(dataSource.getMetadata(User))</code>), and the registry&#39;s <code>schemas</code> options can be keyed by entity class via a <code>Map</code> instead of the derived name. An options key that matches no entity throws, so entity renames fail loudly.</p><p>To mix hand-written and derived schemas, pass an existing registry — entities whose derived name is already registered are skipped, so the hand-written schema stays authoritative and derivation fills in the rest:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> registry</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> SchemaRegistry</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">registry.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(userSchema);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// curated by hand</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineSchemaRegistryWithDataSource</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dataSource, { registry });</span></span></code></pre></div><p>Passing <code>schemas</code> options for a skipped (already registered) name throws — options that would be silently ignored are treated as a mistake.</p><p>Derivation never sets <a href="/guide/schemas.html#strict-mode"><code>strict</code></a> — combined with <code>strict: true</code>, a derived <code>allowed: &#39;inherit&#39;</code> opens <strong>every</strong> (non-hidden) column to clients, so opt sensitive resources into explicit lists instead.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>The data source only needs built metadata, not an open connection — deriving schemas at startup before <code>dataSource.initialize()</code> completes is fine as long as the metadata was built.</p></div><h2 id="applying-a-single-parameter" tabindex="-1">Applying a single parameter <a class="header-anchor" href="#applying-a-single-parameter" aria-label="Permalink to &quot;Applying a single parameter&quot;">​</a></h2><p>A <code>Query</code> with only some parameters set applies just those — the rest are empty and become no-ops. To apply, say, only the filters of a parsed query:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Query } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TypeormAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ queryBuilder });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ filters: query.filters }));</span></span></code></pre></div><p>For lower-level control, each per-parameter sub-adapter (<code>adapter.filters</code>, <code>adapter.fields</code>, <code>adapter.sort</code>, <code>adapter.pagination</code>, <code>adapter.relations</code>) pairs with the matching <code>@rapiq/sql</code> visitor (<code>FiltersVisitor</code>, <code>FieldsVisitor</code>, <code>SortsVisitor</code>, <code>PaginationVisitor</code>, <code>RelationsVisitor</code>) and applies via its own <code>execute()</code> — the query builder is already bound from the adapter&#39;s construction.</p><h2 id="end-to-end-example" tabindex="-1">End-to-end example <a class="header-anchor" href="#end-to-end-example" aria-label="Permalink to &quot;End-to-end example&quot;">​</a></h2><p>The complete Express endpoint — schemas, decoding, error handling, response <code>meta</code> — lives in the <a href="/guide/recipes/express-typeorm.html">Express &amp; TypeORM recipe</a>.</p><div class="info custom-block"><p class="custom-block-title">Migrating from typeorm-extension</p><p><code>applyQuery</code> used <code>leftJoinAndSelect</code> and returned the parsed pagination — <code>joinType: &#39;left&#39;</code> (the default) and the <code>execute(query)</code> return value mirror that contract. See the <a href="/guide/migration-typeorm-extension.html">migration guide</a>.</p></div>`,41)])])}const y=e(n,[["render",l]]);export{k as __pageData,y as default};
