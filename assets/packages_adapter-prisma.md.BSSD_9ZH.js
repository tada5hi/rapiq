import{_ as e,o as i,c as a,a2 as t}from"./chunks/framework.CpXckY1Z.js";const k=JSON.parse('{"title":"@rapiq/adapter-prisma","description":"","frontmatter":{},"headers":[],"relativePath":"packages/adapter-prisma.md","filePath":"packages/adapter-prisma.md","lastUpdated":1785668989000}'),n={name:"packages/adapter-prisma.md"};function l(h,s,r,p,d,o){return i(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="rapiq-adapter-prisma" tabindex="-1">@rapiq/adapter-prisma <a class="header-anchor" href="#rapiq-adapter-prisma" aria-label="Permalink to &quot;@rapiq/adapter-prisma&quot;">​</a></h1><p>Serializes a parsed <a href="/guide/query-ast.html"><code>Query</code></a> into a <strong>Prisma argument object</strong>: filters become <code>where</code>, fields become <code>select</code>, relations become <code>include</code>, sort becomes <code>orderBy</code> and pagination becomes <code>take</code>/<code>skip</code>.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/core</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/adapter-prisma</span></span></code></pre></div><p>Unlike the TypeORM adapter there is nothing to mutate: the adapter is a pure serializer that returns a plain object. <code>@prisma/client</code> is neither a runtime nor a peer dependency of this package. It is a devDependency only, because the test suite runs the emitted arguments through a real engine.</p><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { PrismaAdapter, defineMetadata } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-prisma&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PrismaAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    model: prisma.user,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // the model facts; hand-written on prisma 7 (see Model metadata)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    metadata: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineMetadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(datamodel, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;User&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">args</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pagination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> users</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> prisma.user.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findMany</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(args);</span></span></code></pre></div><p>A model delegate binds the model name, the active provider and the runners; <code>metadata</code> supplies the model facts the serializer needs (see <a href="#model-metadata">Model metadata</a> for the shape and why). On Prisma 6 classic builds the datamodel can also be read off the client, so the delegate alone suffices there; Prisma 7 prunes the runtime datamodel, which is why the example passes it explicitly. <code>pagination</code> echoes the limit/offset actually applied, e.g. for a response <code>meta</code> block.</p><p>Bind your generated argument type so the call site stays type-checked:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PrismaAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Prisma</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">UserFindManyArgs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({ </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* … */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre></div><p>Because the adapter produces a value instead of writing into a builder, it is <strong>stateless</strong>: construct one instance per model and share it across requests. To combine several queries, compose them <em>before</em> serializing with <a href="/guide/merging-queries.html"><code>mergeQueries</code></a> or <code>query.filters.and(...)</code>; the adapter deliberately has no accumulation API.</p><h2 id="running-the-query" tabindex="-1">Running the query <a class="header-anchor" href="#running-the-query" aria-label="Permalink to &quot;Running the query&quot;">​</a></h2><p>A model-bound adapter can also run what it serialized, the counterpart of the typeorm adapter applying its state to the bound query builder:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rows</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findMany</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> total</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">count</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span></code></pre></div><p><code>count</code> sees the query&#39;s filters (and any baseline <code>where</code>) but never the page window: the pre-pagination total a response meta block reports. The two compose however your endpoint needs them; there is deliberately no bundled rows-plus-total call, because it would hide a second query and, without a transaction, could pair mutually inconsistent results. On an adapter constructed with explicit <code>{ provider, metadata }</code> the runners reject with a typed error, since there is nothing to run against; <code>execute()</code> stays the pure serializer either way.</p><h2 id="merging-arguments" tabindex="-1">Merging arguments <a class="header-anchor" href="#merging-arguments" aria-label="Permalink to &quot;Merging arguments&quot;">​</a></h2><p>Prisma ships no per-call args composition (<code>$extends</code> intercepts every call globally), so the merge rules the adapter applies to its <code>base</code> option are exported as a standalone helper:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { mergeArgs } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-prisma&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mergeArgs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(baseline, produced);</span></span></code></pre></div><p><code>where</code> conditions are conjoined (<code>AND</code>), an overriding <code>include</code> joins a baseline <code>select</code> instead of replacing it (a caller-owned projection is never widened), <code>orderBy</code>/<code>take</code>/<code>skip</code> follow the override, and unknown keys (<code>cursor</code>, <code>distinct</code>, ...) pass through. The adapter&#39;s <code>execute(query, { base })</code> is this merge applied to what the query produced, with one exception: an unsatisfiable condition (e.g. <code>in([])</code>) keeps its root-level <code>{ OR: [] }</code> form beside the baseline instead of being conjoined, because prisma strips empty groups below the root.</p><h2 id="model-metadata" tabindex="-1">Model metadata <a class="header-anchor" href="#model-metadata" aria-label="Permalink to &quot;Model metadata&quot;">​</a></h2><p>The adapter needs four facts about your model that a <code>Query</code> cannot carry, and each one changes what a <em>valid</em> Prisma filter looks like:</p><table tabindex="0"><thead><tr><th>fact</th><th>what it decides</th></tr></thead><tbody><tr><td>is the path a relation?</td><td>Prisma rejects <code>not: null</code> on a relation: absence is a presence test</td></tr><tr><td>is that relation to-many?</td><td><code>some</code>/<code>none</code> versus <code>is</code>; the wrong one is a validation error</td></tr><tr><td>can the column hold <code>null</code>?</td><td>a null comparison on a required column is a validation error</td></tr><tr><td>does it hold strings?</td><td><code>mode: &#39;insensitive&#39;</code> exists only on string filters</td></tr></tbody></table><p>Guessing any of them produces a runtime validation error rather than graceful degradation, so the adapter refuses to run without them. On Prisma 6 classic builds the client-bound form above derives all of it; on Prisma 7 the runtime datamodel no longer carries cardinality or nullability, so the binding takes <code>metadata</code> alongside the delegate. The fully explicit form takes the same facts by hand:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Prisma } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@prisma/client&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { PrismaAdapter, defineMetadata } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-prisma&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PrismaAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    provider: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;postgresql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    metadata: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineMetadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Prisma.dmmf.datamodel, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;User&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Between the two sit <code>{ client: prisma, model: &#39;User&#39; }</code> and per-option overrides (<code>provider</code>, <code>metadata</code>) on the client-bound shape.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>The client-bound form reads <code>_activeProvider</code>, the delegate&#39;s <code>$name</code>/<code>$parent</code> backrefs and, on Prisma 6 classic builds, <code>_runtimeDataModel</code>: private but long-stable client internals (Prisma has no public reflection API, <a href="https://github.com/prisma/prisma/issues/19392" target="_blank" rel="noreferrer">prisma#19392</a>). The engine-backed test suite pins them against real generated clients, and every read fails typed rather than guessing. The explicit form is the private-API-free path. When a client exposes the public <code>$datamodel</code>/<code>$provider</code> reflection surface proposed there (<a href="https://github.com/prisma/prisma/pull/29792" target="_blank" rel="noreferrer">prisma#29792</a>), the adapter prefers it over the private reads.</p></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>Prisma 7 prunes every runtime datamodel to names and kinds (<code>Prisma.dmmf</code> included), as the v6 edge/wasm builds and the new <code>prisma-client</code> generator already did; the adapter rejects pruned input typed. <code>defineMetadata</code> accepts any object of the shape <code>{ models: [{ name, fields: [{ name, kind, isList, isRequired, type }] }] }</code>, so a hand-written datamodel works everywhere, on every Prisma version.</p></div><h2 id="parameter-mapping" tabindex="-1">Parameter mapping <a class="header-anchor" href="#parameter-mapping" aria-label="Permalink to &quot;Parameter mapping&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Query parameter</th><th>Prisma argument</th></tr></thead><tbody><tr><td><code>filters</code></td><td><code>where</code></td></tr><tr><td><code>fields</code></td><td><code>select</code></td></tr><tr><td><code>relations</code></td><td><code>include</code> (nested <code>select</code> when fields are picked)</td></tr><tr><td><code>sort</code></td><td><code>orderBy</code> (array of single-key objects, order preserved)</td></tr><tr><td><code>pagination</code></td><td><code>take</code> / <code>skip</code></td></tr></tbody></table><p>Prisma rejects <code>select</code> and <code>include</code> on the same level, so the adapter emits exactly one of them per level: a level that picks fields is projected sparsely with <code>select</code>, a level that only widens with relations uses <code>include</code>. An explicitly included relation without direct picks is hydrated whole; a per-relation fieldset (direct <code>relation.field</code> picks) narrows the include to exactly those columns, matching the projection contract of <a href="/packages/adapter-memory.html">@rapiq/adapter-memory</a> (<a href="https://github.com/tada5hi/rapiq/issues/847" target="_blank" rel="noreferrer">#847</a>). Picks belonging to a deeper relation never narrow the traversed prefix.</p><p>Every column a <a href="/guide/schemas.html#condition-verdicts">field visibility gate</a> reads is force-projected into the <code>select</code> (the gates themselves are enforced post-fetch via <code>@rapiq/adapter-memory</code>&#39;s <code>applyFieldConditions</code>, exactly as with the SQL backends). Those operand entries never narrow an include: behind a pick-free whole relation the hydration covers them anyway.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fields: [&#39;id&#39;, &#39;realm.name&#39;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">select</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">realm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">select</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } } }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fields: [&#39;id&#39;], relations: [&#39;realm&#39;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">select</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">realm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// relations: [&#39;items&#39;, &#39;items.realm&#39;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">include</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">include</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">realm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } } }</span></span></code></pre></div><h2 id="schema-derivation" tabindex="-1">Schema derivation <a class="header-anchor" href="#schema-derivation" aria-label="Permalink to &quot;Schema derivation&quot;">​</a></h2><p>The datamodel can also supply the <em>shape</em> of your schemas, the same staging as <a href="/packages/adapter-typeorm.html">@rapiq/adapter-typeorm</a>&#39;s entity derivation: the schema name, the relation allow-list and the <code>schemaMapping</code> used for relation traversal are derived, while authorization stays explicit. A derived schema without per-parameter options allows nothing.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineSchemaRegistryWithDatamodel } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-prisma&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> registry</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineSchemaRegistryWithDatamodel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Prisma.dmmf.datamodel, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    schemas: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        user: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            fields: { allowed: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;inherit&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the model&#39;s field names</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            filters: { allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// authorization stays yours</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>One schema per model is registered under the lower-camel model name; hand-written schemas already in the registry take precedence. <code>defineSchemaWithModel(datamodel, &#39;User&#39;, options)</code> derives a single schema, and the <code>&#39;inherit&#39;</code> sentinel expands to the model&#39;s scalar and enum field names.</p><p>To catch schema/model drift (a renamed field, a stale allow-list entry) at boot time instead of as a dead entry:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { assertSchemaMatchesModel } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-prisma&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertSchemaMatchesModel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(schema, Prisma.dmmf.datamodel, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;User&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// throws SchemaModelMismatchError carrying EVERY offending key</span></span></code></pre></div><h2 id="preserving-an-application-owned-predicate" tabindex="-1">Preserving an application-owned predicate <a class="header-anchor" href="#preserving-an-application-owned-predicate" aria-label="Permalink to &quot;Preserving an application-owned predicate&quot;">​</a></h2><p>Rapiq filters <strong>narrow</strong> a query, they never replace it. Hand the adapter a baseline argument object and the client&#39;s conditions are conjoined with your tenant or authorization scope:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">args</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    base: { where: { realm_id: realmId } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// where: { AND: [ { realm_id: … }, { …client filters } ] }</span></span></code></pre></div><p>Baseline keys the query does not produce (<code>take</code>, <code>orderBy</code>, …) survive untouched: the same preservation contract <a href="/packages/adapter-typeorm.html">@rapiq/adapter-typeorm</a> applies to a query builder.</p><h2 id="negation" tabindex="-1">Negation <a class="header-anchor" href="#negation" aria-label="Permalink to &quot;Negation&quot;">​</a></h2><p>rapiq&#39;s negated operators (<code>ne</code>, <code>nin</code>, <code>notContains</code>, …) and <code>not(...)</code> groups are the <strong>exact null-inclusive complement</strong> of their positive twin: they match precisely the records the positive form does not. Prisma&#39;s <code>not</code> and <code>NOT</code> instead follow three-valued SQL logic and silently drop rows whose column is <code>NULL</code>.</p><p>The adapter therefore never delegates negation to Prisma. It pushes negation down to the leaves (De Morgan) and renders each complement itself:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ne(&#39;address&#39;, &#39;Mordor&#39;)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">OR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [ { address: { not: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Mordor&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }, { address: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } ] }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ne(&#39;realm.name&#39;, &#39;master&#39;): realm is an optional to-one relation</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">OR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { realm: { is: { name: { not: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;master&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { NOT: { realm: { is: {} } } },      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// …or there is no realm at all</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }</span></span></code></pre></div><p>The result set is identical to <code>@rapiq/adapter-sql</code>, <code>@rapiq/adapter-typeorm</code> and <code>@rapiq/adapter-memory</code>; the complement law is pinned by a cross-backend test suite. When metadata proves a column non-nullable the null arm is dropped, because no null can exist there.</p><p>A <strong>to-many</strong> path quantifies existentially, for the negated form too: the SQL backends evaluate the condition once per joined row, so a record with one matching and one non-matching element satisfies both a condition and its negation:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ne(&#39;items.title&#39;, &#39;book&#39;): items is to-many</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">OR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { items: { some: { title: { not: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;book&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { items: { none: {} } },             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// an empty collection is one null row</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }</span></span></code></pre></div><p><strong>Same-element binding holds.</strong> Two conditions on one to-many path (<code>filter[items.title]</code> and <code>filter[items.color]</code>) bind to the <em>same</em> joined row in the SQL backends, so the adapter factors them into a single <code>some</code> scope instead of emitting independent existentials:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// and(eq(&#39;items.title&#39;, &#39;book&#39;), eq(&#39;items.color&#39;, &#39;red&#39;))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">some</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { title: { equals: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;book&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { color: { equals: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;red&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] } } }</span></span></code></pre></div><p>A record whose elements satisfy the two conditions only separately does not match, on any backend. Trees mixing root and relation conditions are expanded distributively first, so the guarantee also covers nested expression-dialect input. <a href="/guide/filters.html#elemmatch"><code>elemMatch</code></a> remains the explicit same-element construct.</p><h2 id="case-sensitivity" tabindex="-1">Case sensitivity <a class="header-anchor" href="#case-sensitivity" aria-label="Permalink to &quot;Case sensitivity&quot;">​</a></h2><p>String comparison is <a href="/guide/filters.html#case-sensitivity">case-insensitive by default</a> across all rapiq backends. Prisma expresses that with <code>mode: &#39;insensitive&#39;</code>, which only some connectors accept, so the adapter takes a provider:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PrismaAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ provider: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mysql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre></div><table tabindex="0"><thead><tr><th>provider</th><th>behavior</th></tr></thead><tbody><tr><td><code>postgresql</code>, <code>cockroachdb</code>, <code>mongodb</code></td><td><code>mode: &#39;insensitive&#39;</code> is emitted</td></tr><tr><td><code>mysql</code>, <code>sqlserver</code></td><td>nothing emitted: the default <code>*_ci</code> collation already compares case-insensitively</td></tr><tr><td><code>sqlite</code></td><td>nothing emitted: <code>contains</code>/<code>startsWith</code>/<code>endsWith</code> are ASCII-case-insensitive, <code>equals</code>/<code>in</code> are <strong>not</strong></td></tr></tbody></table><p><code>provider</code> is required, and an unrecognized name throws rather than falling back: emitting <code>mode</code> on a connector without support is a Prisma validation error on <em>every</em> case-insensitive filter, which is too sharp an edge to guess at.</p><p>Two narrowing rules apply on top. A non-string column never folds. And an <strong>equality</strong> whose value contains <code>%</code> or <code>_</code> compares case-sensitively instead, because Prisma lowers an insensitive <code>equals</code> to <code>ILIKE</code> with the operand passed through verbatim (<a href="https://github.com/prisma/prisma/issues/20318" target="_blank" rel="noreferrer">#20318</a>, <a href="https://github.com/prisma/prisma/issues/10810" target="_blank" rel="noreferrer">#10810</a>): measured on Postgres 18:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">address</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">equals</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;a_b&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;insensitive&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// also matches &#39;axb&#39;, &#39;a%b&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">address</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">equals</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;a%b&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;insensitive&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// matches everything</span></span></code></pre></div><p>Matching too little is the safer failure, so those values keep the exact comparison. The rule is scoped to equality on purpose: <code>in</code>/<code>notIn</code> are never lowered to <code>ILIKE</code> and stay case-insensitive, while <code>contains</code>/<code>startsWith</code>/<code>endsWith</code> are <code>LIKE</code>-based with <em>or without</em> <code>mode</code>: that is Prisma&#39;s own behaviour, not something this adapter introduces.</p><p>Opt individual fields out through the schema:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: { allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;token&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], caseSensitive: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;token&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h2><p>Operators without a Prisma equivalent raise a typed <code>AdapterError</code> (<code>FEATURE_UNSUPPORTED</code>) instead of being approximated:</p><ul><li><code>regex</code>: no connector exposes a regular-expression filter</li><li><code>mod</code>: no modulo filter</li><li><code>size</code>: no array-length filter</li><li>the <code>$this</code> element marker: the element itself is not addressable</li></ul><p>Four further deviations are worth knowing:</p><ul><li><strong>Ordering by a to-many relation</strong> is not expressible in Prisma (only <code>_count</code> is). Such a sort is emitted as written and rejected by Prisma.</li><li><strong>Same-element binding.</strong> Two conditions on the same to-many path (<code>filter[items.title]</code> and <code>filter[items.color]</code>) bind to the <em>same</em> joined row in the SQL backends, but Prisma evaluates each <code>some</code> independently, so they may match different elements. Use <a href="/guide/filters.html#elemmatch"><code>elemMatch</code></a> when the conditions must hold for one and the same element; it maps to a single <code>some</code> scope and agrees on every backend.</li><li><strong><code>elemMatch</code> targets a to-many relation.</strong> Applying it to a to-one relation emits <code>some</code>, which Prisma rejects.</li><li><strong>SQLite compares case-sensitively.</strong> It has no <code>mode: &#39;insensitive&#39;</code> and its <code>=</code> is case-sensitive, so the case-insensitive default does not hold there. <code>contains</code>/<code>startsWith</code>/<code>endsWith</code> are ASCII-case-insensitive via <code>LIKE</code>. Column-level <code>COLLATE NOCASE</code> is the fix. This is measured by the engine suite, not assumed.</li></ul><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p><code>npm run test:db</code> runs the whole parity matrix through a real Prisma client: against SQLite by default, and against PostgreSQL when <code>DB_TYPE=postgres</code> names a live server (CI runs both). Every condition is executed three ways: through the engine, through <a href="/packages/adapter-memory.html">@rapiq/adapter-memory</a>, and through an in-test evaluator, and all three must select the same records. The connector-specific claims on this page (the case contract, the wildcard behaviour) are measurements from that suite rather than assumptions.</p><p>A note on <code>exists()</code>: it asks whether a <em>value is present</em>, so on a to-many relation it is always true: a collection is possibly empty, never absent. That matches <a href="/packages/adapter-memory.html">@rapiq/adapter-memory</a>; &quot;has no elements&quot; is a different question that <code>exists</code> does not ask.</p><h2 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-label="Permalink to &quot;Documentation&quot;">​</a></h2><ul><li><a href="/guide/filters.html">Filters</a>: operator reference and case semantics</li><li><a href="/guide/executing-queries.html">Executing Queries</a>: the adapter surface shared by all backends</li></ul>`,71)])])}const g=e(n,[["render",l]]);export{k as __pageData,g as default};
