import{_ as s,o as i,c as a,a2 as t}from"./chunks/framework.CpXckY1Z.js";const k=JSON.parse('{"title":"@rapiq/adapter-sql","description":"","frontmatter":{},"headers":[],"relativePath":"packages/adapter-sql.md","filePath":"packages/adapter-sql.md","lastUpdated":1790085945000}'),n={name:"packages/adapter-sql.md"};function d(o,e,l,h,r,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="rapiq-adapter-sql" tabindex="-1">@rapiq/adapter-sql <a class="header-anchor" href="#rapiq-adapter-sql" aria-label="Permalink to &quot;@rapiq/adapter-sql&quot;">​</a></h1><p>Turns query AST nodes into <strong>parameterized SQL fragments</strong>. Database-agnostic: per-database behavior is injected as a small dialect option object, not a subclass. It is also the foundation the <a href="/packages/adapter-typeorm.html">TypeORM adapter</a> builds on.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/core</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/adapter-sql</span></span></code></pre></div><h2 id="dialects" tabindex="-1">Dialects <a class="header-anchor" href="#dialects" aria-label="Permalink to &quot;Dialects&quot;">​</a></h2><p>A dialect is a handful of callbacks:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DialectOptions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    escapeField</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// mysql: \`field\`, pg: &quot;field&quot;, mssql: [field]</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    paramPlaceholder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">index</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// pg: $1, mysql: ?</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    regexp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">field</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">placeholder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">ignoreCase</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> boolean</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    caseFold</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default: lower(input); mysql/mssql: identity</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    caseFoldLike</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">input</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// default: caseFold; sqlite: identity</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    mod</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">field</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">divisorPlaceholder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">remainderPlaceholder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // default: mod(field, divisor) = remainder; mssql: field % divisor = remainder</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Presets ship for <code>pg</code>, <code>mysql</code>, <code>sqlite</code>, <code>mssql</code> and <code>oracle</code>:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { mysql, pg } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-sql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p><code>resolveDialect(name)</code> maps a driver/connection type name (e.g. TypeORM&#39;s <code>connection.options.type</code> or a knex client name) to the matching preset (<code>postgres</code>, <code>mariadb</code>, <code>better-sqlite3</code>, <code>oracledb</code>, …) and returns <code>undefined</code> for unknown names:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { resolveDialect } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-sql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">resolveDialect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mariadb&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// mysql preset</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Dialects without regex support</p><p>The <code>mssql</code> and <code>sqlite</code> presets omit the <code>regexp</code> callback: SQL Server has no regex operator, and stock SQLite ships without a <code>REGEXP</code> function. The callback serves the <code>regex</code> filter operator <strong>only</strong>, so on those two presets that operator throws a typed <code>AdapterError</code> (<code>ErrorCode.FEATURE_UNSUPPORTED</code>) and nothing else is affected. The <code>contains</code> / <code>startsWith</code> / <code>endsWith</code> operators render as <code>LIKE</code> on every preset, regex-capable or not (see <a href="#string-matching">String matching</a>).</p></div><p>No single <code>mod</code> spelling works everywhere: <code>pg</code>, <code>mysql</code>, <code>sqlite</code> and <code>oracle</code> render <code>mod(field, divisor) = remainder</code> (a <code>MOD()</code> function or equivalent); <code>mssql</code> has no <code>MOD()</code> function, so its preset renders <code>field % divisor = remainder</code> instead, using SQL Server&#39;s modulo operator. A custom dialect that omits the <code>mod</code> callback raises a typed <code>AdapterError</code> (<code>ErrorCode.FEATURE_UNSUPPORTED</code>, feature <code>filters:mod</code>) for the <code>mod</code> filter operator, exactly like an omitted <code>regexp</code>.</p><div class="warning custom-block"><p class="custom-block-title">SQLite needs a build with math functions</p><p>SQLite&#39;s <code>mod()</code> is a <a href="https://sqlite.org/lang_mathfunc.html" target="_blank" rel="noreferrer">math function</a>: available since SQLite 3.35, and only in builds compiled with <code>SQLITE_ENABLE_MATH_FUNCTIONS</code>. Every mainstream Node driver enables it (<code>better-sqlite3</code>, <code>node:sqlite</code>, <code>sqlite3</code>), so the preset renders <code>mod(...)</code>; a build without it reports <code>no such function: mod</code>. Some embedded targets the preset also covers (<code>sql.js</code>, <code>cordova</code>, <code>react-native</code>, <code>capacitor</code>, <code>expo</code>, <code>nativescript</code>) make no such guarantee, so override the callback there:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { sqlite } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-sql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> dialect</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    ...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sqlite,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // \`%\` is core syntax and always available, but SQLite casts both</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // operands to INTEGER: \`5.5 % 2\` is 1, whereas \`mod(5.5, 2)\` is 1.5.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // Only equivalent for integer operands.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    mod</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">field</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">divisor</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">remainder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> \`\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">field</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">} % \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">divisor</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">} = \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">remainder</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><p>Setting <code>mod: undefined</code> instead turns the operator into the typed <code>filters:mod</code> refusal, which is the safer choice when non-integer operands are possible: <code>@rapiq/adapter-memory</code> evaluates <code>mod</code> with JavaScript&#39;s <code>%</code> (float-capable), so an integer-casting <code>%</code> would answer differently for the same query.</p></div><h2 id="the-root-adapter" tabindex="-1">The root adapter <a class="header-anchor" href="#the-root-adapter" aria-label="Permalink to &quot;The root adapter&quot;">​</a></h2><p>Each parameter has an adapter/visitor pair (<code>FieldsAdapter</code>/<code>FieldsVisitor</code>, <code>SortsAdapter</code>/<code>SortsVisitor</code>, <code>PaginationAdapter</code>/<code>PaginationVisitor</code>, <code>RelationsAdapter</code>/<code>RelationsVisitor</code>) that collects the walked state: selected columns, order map, limit/offset, relation paths. A root <code>Adapter</code> bundles all five; <code>execute(query)</code> walks a whole <code>Query</code> into it and returns the accumulated clause fragments:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Adapter, pg } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-sql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Adapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">pg, rootAlias: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fragments</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     columns: [&#39;&quot;user&quot;.&quot;id&quot;&#39;, &#39;&quot;user&quot;.&quot;name&quot;&#39;, &#39;&quot;r5_realm&quot;.&quot;name&quot;&#39;],</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     where: &#39;(&quot;user&quot;.&quot;age&quot; &gt;= $1 and ...)&#39;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     params: [18, ...],</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     orderBy: [&#39;&quot;user&quot;.&quot;age&quot; DESC&#39;],</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     limit: 25,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     offset: 50,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     relations: [&#39;realm&#39;],</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // canonical paths, parents included</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// }</span></span></code></pre></div><p>Construct the <code>Adapter</code> <strong>per request</strong>: it accumulates per-call state; the shareable, long-lived part is the options object.</p><p><code>@rapiq/adapter-sql</code> deliberately stops at fragments: composing the final <code>SELECT</code> statement (in particular <code>FROM</code>/<code>JOIN</code> conditions, which require knowledge of the table layout) is the job of the caller or a backend adapter. That&#39;s exactly what <a href="/packages/adapter-typeorm.html"><code>@rapiq/adapter-typeorm</code></a> does for TypeORM.</p><div class="warning custom-block"><p class="custom-block-title">Alias convention</p><p>Fragments reference joined columns through the exported <code>buildRelationAlias(path)</code> derivation. It length-prefixes every path segment (<code>realm</code> → <code>r5_realm</code>, <code>role.realm</code> → <code>r4_role_5_realm</code>), so <code>role_realm</code> and <code>role.realm</code> cannot collapse onto one alias. Use the same helper when rendering <code>JOIN</code> clauses from <code>relations</code>, or inject one convention through the <code>relationAlias</code> adapter option. Keep a custom derivation collision-free and within your database&#39;s identifier length limit.</p></div><h3 id="dotted-paths-relations" tabindex="-1">Dotted paths &amp; relations <a class="header-anchor" href="#dotted-paths-relations" aria-label="Permalink to &quot;Dotted paths &amp; relations&quot;">​</a></h3><p>A dotted field path (<code>realm.name</code>) references a joined relation by default: the prefix registers with the relations adapter and the fragment renders against the derived join alias. Backends where a dotted prefix is not necessarily a relation override <code>isRelationPath(path)</code> on the relations adapter (default: <code>true</code>); segments only count as a relation path while the hook confirms them, and the remainder stays part of the column name, rendered against the parent alias (the root alias, or the last confirmed relation&#39;s join alias). The <a href="/packages/adapter-typeorm.html">TypeORM adapter</a> implements the hook via entity metadata so <a href="/packages/adapter-typeorm.html#embedded-columns">embedded column paths</a> such as <code>profile.firstName</code> don&#39;t produce a bogus join.</p><h2 id="rendering-filters-standalone" tabindex="-1">Rendering filters standalone <a class="header-anchor" href="#rendering-filters-standalone" aria-label="Permalink to &quot;Rendering filters standalone&quot;">​</a></h2><p>The filters adapter accumulates conditions while a visitor walks the tree, then hands back SQL plus bound parameters:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    FiltersAdapter, FiltersVisitor, RelationsAdapter, pg,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-sql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> filters</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FiltersAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RelationsAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(), pg);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">query.filters.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">accept</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FiltersVisitor</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(filters));</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">sql</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">params</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> filters.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getQueryAndParameters</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// sql:    (lower(&quot;name&quot;) like lower($1) escape &#39;!&#39; and &quot;age&quot; &gt;= $2)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// params: [&#39;%jo%&#39;, 18]</span></span></code></pre></div><p>Values are always bound as parameters, never interpolated into the SQL string.</p><h3 id="null-semantics" tabindex="-1">Null semantics <a class="header-anchor" href="#null-semantics" aria-label="Permalink to &quot;Null semantics&quot;">​</a></h3><p>A <code>null</code> filter value is rewritten to the SQL null predicates instead of being bound as a parameter (which would match nothing):</p><table tabindex="0"><thead><tr><th>Filter</th><th>SQL</th></tr></thead><tbody><tr><td><code>eq(field, null)</code></td><td><code>field IS NULL</code></td></tr><tr><td><code>ne(field, null)</code></td><td><code>field IS NOT NULL</code></td></tr><tr><td><code>in(field, [a, null])</code></td><td><code>(field IN (...) OR field IS NULL)</code></td></tr><tr><td><code>nin(field, [a, null])</code></td><td><code>(field NOT IN (...) AND field IS NOT NULL)</code></td></tr></tbody></table><p>An empty list never matches: <code>in(field, [])</code> renders <code>1 = 0</code> and <code>nin(field, [])</code> renders <code>1 = 1</code> (instead of the invalid <code>IN ()</code>).</p><p>Negated operators are <strong>exact complements</strong> of their positive twins: a record that does not match <code>eq(field, a)</code> matches <code>ne(field, a)</code>, including records where the column is <code>NULL</code>. Since a bare SQL negation drops <code>NULL</code> rows under three-valued logic, negations render null-inclusively:</p><table tabindex="0"><thead><tr><th>Filter</th><th>SQL</th></tr></thead><tbody><tr><td><code>ne(field, a)</code></td><td><code>(field &lt;&gt; ? OR field IS NULL)</code></td></tr><tr><td><code>nin(field, [a, b])</code></td><td><code>(field NOT IN (...) OR field IS NULL)</code></td></tr><tr><td><code>notContains(field, a)</code> (also <code>notStartsWith</code> / <code>notEndsWith</code>)</td><td><code>(field NOT LIKE ? ESCAPE &#39;!&#39; OR field IS NULL)</code></td></tr></tbody></table><h3 id="string-matching" tabindex="-1">String matching <a class="header-anchor" href="#string-matching" aria-label="Permalink to &quot;String matching&quot;">​</a></h3><p>The <code>contains</code> / <code>startsWith</code> / <code>endsWith</code> operators (and their negations) render as <code>LIKE</code> on <strong>every</strong> dialect, with the value escaped and wrapped into the matching pattern:</p><table tabindex="0"><thead><tr><th>Filter</th><th>Pattern</th><th>SQL (pg)</th></tr></thead><tbody><tr><td><code>startsWith(field, &#39;foo&#39;)</code></td><td><code>foo%</code></td><td><code>lower(&quot;field&quot;) like lower($1) escape &#39;!&#39;</code></td></tr><tr><td><code>endsWith(field, &#39;foo&#39;)</code></td><td><code>%foo</code></td><td><code>lower(&quot;field&quot;) like lower($1) escape &#39;!&#39;</code></td></tr><tr><td><code>contains(field, &#39;foo&#39;)</code></td><td><code>%foo%</code></td><td><code>lower(&quot;field&quot;) like lower($1) escape &#39;!&#39;</code></td></tr><tr><td><code>notContains(field, &#39;foo&#39;)</code></td><td><code>%foo%</code></td><td><code>(lower(&quot;field&quot;) not like lower($1) escape &#39;!&#39; or &quot;field&quot; is null)</code></td></tr></tbody></table><p>Previously the regex-capable presets built an anchored regular expression instead (<code>field ~* &#39;^foo&#39;</code>). <code>LIKE</code> replaced it because a regex predicate can cost an index: measured on MySQL 9.7.1 over 50k rows with a btree index, <code>col regexp &#39;^sales/&#39; = 1</code> examined all 50,000 rows (a covering index scan) where <code>col like &#39;sales/%&#39;</code> examined 50 (a range scan). On Postgres 18.4 the two are equivalent (<code>col ~ &#39;^sales/&#39;</code> and <code>col like &#39;sales/%&#39;</code> produce the identical Index Cond on a <code>text_pattern_ops</code> index), so nothing is lost there either. <code>LIKE</code> is also what every other backend adapter emits, so the rendering is uniform now. The <code>regexp</code> dialect callback stays reserved for the <code>regex</code> operator alone.</p><p>The fold is not free either: <code>lower()</code> copies the whole column value per row, where the regex could stop at the anchor, so an unindexed <code>startsWith</code> over a wide text column is measurably slower folded than it was as a regex. List such a column in <code>caseSensitive</code> when exactness is acceptable.</p><p>Only <code>startsWith</code> can win a <strong>btree</strong> index that way: <code>contains</code> (<code>%foo%</code>) and <code>endsWith</code> (<code>%foo</code>) cannot use one, exactly as they could not under a regex.</p><div class="warning custom-block"><p class="custom-block-title">Postgres trigram indexes</p><p><code>pg_trgm</code> is the exception, and the one configuration this change makes slower. A <code>gin (col gin_trgm_ops)</code> index serves <code>col ~* &#39;?&#39;</code> and <code>col LIKE &#39;%?%&#39;</code>, but not <code>lower(col) LIKE lower(&#39;%?%&#39;)</code>: measured on Postgres 18.4 over 100k rows, <code>contains</code> went from a 0.9 ms bitmap index scan to a 22 ms sequential scan. Index the folded expression instead, <code>CREATE INDEX ON &quot;user&quot; USING gin (lower(path) gin_trgm_ops)</code>, or list the column in <code>caseSensitive</code>.</p></div><p>The value is matched <strong>literally</strong>: <code>%</code>, <code>_</code> and the escape character itself are escaped into the pattern, so <code>contains(field, &#39;100%&#39;)</code> binds <code>&#39;%100!%%&#39;</code> and matches the literal percent sign. <code>[</code> is escaped on the <code>mssql</code> preset only, where it opens a character range (<code>likeBracketWildcard</code>); escaping it elsewhere would make Oracle raise ORA-01424, since Oracle accepts an escape character only before <code>%</code>, <code>_</code> or itself. Only the <code>regex</code> operator interprets its <code>RegExp</code> or string value as a pattern. A JavaScript <code>RegExp</code> contributes its <code>source</code> and <code>ignoreCase</code> flag; a string is passed through unchanged so the selected database regex engine owns its syntax and validation.</p><p>The negations match rows whose column is <code>NULL</code> (complement law, see above).</p><div class="warning custom-block"><p class="custom-block-title">The escape character is <code>!</code>, not a backslash</p><p>Every emitted <code>LIKE</code> carries <code>ESCAPE &#39;!&#39;</code> (exported as <code>LIKE_ESCAPE_CHARACTER</code>). A backslash cannot be spelled statically: MySQL rejects <code>escape &#39;\\&#39;</code> as a syntax error (ERROR 1064) under the default <code>sql_mode</code>, and rejects <code>escape &#39;\\\\&#39;</code> under <code>NO_BACKSLASH_ESCAPES</code>, so no single backslash spelling parses on both. <code>!</code> is never a <code>LIKE</code> metacharacter and was measured equivalent on Postgres, MySQL (both <code>sql_mode</code>s) and SQLite.</p></div><h3 id="case-sensitivity" tabindex="-1">Case sensitivity <a class="header-anchor" href="#case-sensitivity" aria-label="Permalink to &quot;Case sensitivity&quot;">​</a></h3><p>String equality (<code>eq</code> / <code>ne</code> / <code>in</code> / <code>nin</code>) and the anchored operators (<code>contains</code> / <code>startsWith</code> / <code>endsWith</code> and their negations) match <a href="/guide/filters.html#case-sensitivity">case-insensitively by default</a>. On dialects whose comparison is case-sensitive, both sides fold through a dialect callback: <code>caseFold</code> for equality, <code>caseFoldLike</code> for the <code>LIKE</code> comparisons.</p><table tabindex="0"><thead><tr><th>Filter</th><th>pg / oracle</th><th>sqlite</th><th>mysql / mssql</th></tr></thead><tbody><tr><td><code>eq(field, &#39;a&#39;)</code></td><td><code>lower(field) = lower(?)</code></td><td><code>lower(field) = lower(?)</code></td><td><code>field = ?</code></td></tr><tr><td><code>in(field, [&#39;a&#39;, 1])</code></td><td><code>lower(field) IN (lower(?), ?)</code></td><td><code>lower(field) IN (lower(?), ?)</code></td><td><code>field IN (?, ?)</code></td></tr><tr><td><code>startsWith(field, &#39;a&#39;)</code></td><td><code>lower(field) LIKE lower(?) ESCAPE &#39;!&#39;</code></td><td><code>field LIKE ? ESCAPE &#39;!&#39;</code></td><td><code>field LIKE ? ESCAPE &#39;!&#39;</code></td></tr></tbody></table><p>The <code>mysql</code> and <code>mssql</code> presets set <code>caseFold</code> to identity: their default collations (<code>*_ci</code>) already compare case-insensitively, and skipping <code>lower()</code> keeps plain indexes usable. The <code>sqlite</code> preset goes one step finer and sets only <code>caseFoldLike</code> to identity, because SQLite&#39;s <code>LIKE</code> is already ASCII-case-insensitive while its <code>=</code> is not: folding the <code>LIKE</code> would buy nothing semantically (<code>lower()</code> is ASCII-only there too) and would cost the prefix optimisation, which needs an index whose collation matches the comparison (<code>NOCASE</code> under the default <code>case_sensitive_like</code>). <code>caseFoldLike</code> defaults to <code>caseFold</code> when a dialect omits it.</p><p>Fields opted out via the top-level <code>caseSensitive</code> execute option render unfolded on every dialect, for both folds (<code>true</code> opts every field out). Where the dialect skips a fold anyway, the opt-out has nothing left to switch off: on MySQL and MSSQL for both families, and on SQLite for the anchored operators, matching stays collation-governed.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, { caseSensitive: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] });</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Indexing folded columns on Postgres</p><p>The two families need different indexes, because an operator class that serves <code>=</code> does not serve a prefix <code>LIKE</code> under a non-<code>C</code> collation:</p><div class="language-sql vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sql</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- serves lower(name) = lower($1)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> INDEX</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ON</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">lower</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- serves lower(path) LIKE lower($1) with a literal prefix</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> INDEX</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ON</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">lower</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) text_pattern_ops);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- serves path LIKE $1 when the column is listed in caseSensitive</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">CREATE</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> INDEX</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ON</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;user&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> text_pattern_ops);</span></span></code></pre></div><p>Measured on Postgres 18.4: a default <code>en_US.utf8</code> btree serves none of these forms, and the case-insensitive spellings the adapter does <em>not</em> emit (<code>col ~* &#39;^sales/&#39;</code>, <code>col ILIKE &#39;sales/%&#39;</code>) both produced a Seq Scan, which is why the fold is expressed as <code>lower(...)</code> on both sides instead. Only <code>startsWith</code> gains anything either way: <code>%foo%</code> and <code>%foo</code> patterns are full scans regardless.</p></div><p>Equality folds only for string filter values; the anchored operators stringify their value first, so their fold is decided by the column alone. Backends with column metadata can exempt whole columns by overriding <code>isCaseFoldable(field)</code> on the filters adapter (default: <code>true</code>); the <a href="/packages/adapter-typeorm.html">TypeORM adapter</a> uses it to fold only string-typed columns, for <code>LIKE</code> as well as <code>=</code>. A <code>citext</code> column is deliberately not folded there: its own operators already compare case-insensitively, and folding would discard its index.</p><div class="warning custom-block"><p class="custom-block-title">MySQL folds accents too, and now does so for the anchored operators</p><p>Under <code>utf8mb4_0900_ai_ci</code> (the MySQL 8+/9 default), <code>LIKE</code> is accent-insensitive as well as case-insensitive, while <code>REGEXP</code> is neither: <code>&#39;APFEL&#39; LIKE &#39;ä%&#39;</code> is <code>1</code>, but <code>&#39;APFEL&#39; REGEXP &#39;^ä&#39;</code> is <code>0</code>. Rendering the anchored operators as <code>LIKE</code> therefore changes MySQL <strong>result sets</strong>, not only query plans: <code>startsWith(&#39;name&#39;, &#39;ä&#39;)</code> now matches <code>APFEL</code> there. This is the same behaviour <code>eq</code> has always had on MySQL (the preset&#39;s <code>caseFold</code> is identity for the same collation reason). Use a <code>*_as_cs</code> collation on the column when accent and case exactness matter.</p></div><h3 id="itself-element-level-conditions" tabindex="-1">ITSELF (element-level conditions) <a class="header-anchor" href="#itself-element-level-conditions" aria-label="Permalink to &quot;ITSELF (element-level conditions)&quot;">​</a></h3><p>The <a href="/guide/filters.html#operators"><code>ITSELF</code> marker</a> (an <code>elemMatch</code> interior condition on the array element itself, produced e.g. by the mongo parser&#39;s element-level <code>$elemMatch</code> and <code>$all</code>) has no SQL rendering: <code>elemMatch</code> maps to a relation join, and a joined row is not a scalar column. Both <code>@rapiq/adapter-sql</code> and <code>@rapiq/adapter-typeorm</code> throw a typed <code>AdapterError</code> (<code>ErrorCode.FEATURE_UNSUPPORTED</code>). Dialect-level JSON-array support (<code>json_each</code> / <code>unnest</code>) may lift this later; evaluate such filters with <a href="/packages/adapter-memory.html"><code>@rapiq/adapter-memory</code></a> in the meantime.</p><h3 id="size-array-length" tabindex="-1">size (array length) <a class="header-anchor" href="#size-array-length" aria-label="Permalink to &quot;size (array length)&quot;">​</a></h3><p>The <a href="/guide/filters.html#operators"><code>size</code> operator</a> has no SQL rendering either: an array-length check needs per-dialect JSON-array support (<code>json_array_length</code> on Postgres/SQLite, <code>JSON_LENGTH</code> on MySQL, <code>cardinality</code> for Postgres arrays). Both <code>@rapiq/adapter-sql</code> and <code>@rapiq/adapter-typeorm</code> throw a typed <code>AdapterError</code> (<code>ErrorCode.FEATURE_UNSUPPORTED</code>); evaluate such filters with <a href="/packages/adapter-memory.html"><code>@rapiq/adapter-memory</code></a> in the meantime.</p><h2 id="field-visibility-gates" tabindex="-1">Field visibility gates <a class="header-anchor" href="#field-visibility-gates" aria-label="Permalink to &quot;Field visibility gates&quot;">​</a></h2><p>A schema&#39;s <code>fields</code> <a href="/guide/schemas.html#condition-verdicts">validate hook</a> may gate a column with a condition, meaning <em>visible only on rows satisfying it</em>. The rendered SQL cannot express that: a selection stays a bare escaped column, so the column is projected for <strong>every</strong> row and the gate has to be enforced on the fetched rows.</p><p>The gate is compiled from the same AST the adapter walks, so the helper that applies it lives in <a href="/packages/adapter-memory.html"><code>@rapiq/adapter-memory</code></a>, the package that evaluates conditions against plain objects. Install it alongside this one when a schema gates a field:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { applyFieldConditions } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-memory&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> fragments</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rows</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> driver.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assemble</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fragments));</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> output</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> applyFieldConditions</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query.fields, rows);</span></span></code></pre></div><p><code>compileFieldConditions(query.fields)</code> is the single-record form: it compiles the gates once into a reusable <code>(record) =&gt; redacted</code> function. Skipping this step fails open: consumers receive the gated column unredacted.</p>`,59)])])}const g=s(n,[["render",d]]);export{k as __pageData,g as default};
