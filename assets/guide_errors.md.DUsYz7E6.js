import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.CpXckY1Z.js";const k=JSON.parse('{"title":"Error Handling","description":"","frontmatter":{},"headers":[],"relativePath":"guide/errors.md","filePath":"guide/errors.md","lastUpdated":1787001401000}'),r={name:"guide/errors.md"};function n(o,e,d,h,l,c){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="error-handling" tabindex="-1">Error Handling <a class="header-anchor" href="#error-handling" aria-label="Permalink to &quot;Error Handling&quot;">​</a></h1><p>Every error rapiq throws extends <code>BaseError</code> and carries a machine-readable <code>code</code> from <code>ErrorCode</code>: no raw <code>Error</code>s, no string matching. This page maps the hierarchy, the codes, and how to translate them into HTTP responses.</p><h2 id="the-hierarchy" tabindex="-1">The hierarchy <a class="header-anchor" href="#the-hierarchy" aria-label="Permalink to &quot;The hierarchy&quot;">​</a></h2><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BaseError { code: ErrorCode }</span></span>
<span class="line"><span>├── BuildError            defineQuery / helpers: malformed build input</span></span>
<span class="line"><span>├── MergeError            mergeQueries / node merge: discarded field gates</span></span>
<span class="line"><span>├── ParseError            parsers &amp; decoders: invalid client input</span></span>
<span class="line"><span>│   ├── FieldsParseError</span></span>
<span class="line"><span>│   ├── FiltersParseError</span></span>
<span class="line"><span>│   ├── PaginationParseError</span></span>
<span class="line"><span>│   ├── RelationsParseError</span></span>
<span class="line"><span>│   └── SortsParseError</span></span>
<span class="line"><span>├── AdapterError          backends &amp; encoders: query exceeds the target&#39;s subset</span></span>
<span class="line"><span>├── CodecError            codec registry: unresolvable dialect</span></span>
<span class="line"><span>└── SchemaError           schema registry: misconfigured or unresolvable schema</span></span>
<span class="line"><span>    ├── SchemaEntityMismatchError        @rapiq/adapter-typeorm: schema keys unknown to the entity</span></span>
<span class="line"><span>    └── SchemaEntityIndexMismatchError   @rapiq/adapter-typeorm: declared indexes the entity lacks</span></span></code></pre></div><h2 id="where-errors-come-from" tabindex="-1">Where errors come from <a class="header-anchor" href="#where-errors-come-from" aria-label="Permalink to &quot;Where errors come from&quot;">​</a></h2><h3 id="build-time-caller-bug" tabindex="-1">Build time (caller bug) <a class="header-anchor" href="#build-time-caller-bug" aria-label="Permalink to &quot;Build time (caller bug)&quot;">​</a></h3><p><code>defineQuery</code> and the condition helpers throw <code>BuildError</code> on malformed input: an unknown <code>$</code> operator, an invalid key, a bad value shape. These indicate a programming error, not bad user input:</p><table tabindex="0"><thead><tr><th>Code</th><th>Trigger</th></tr></thead><tbody><tr><td><code>INPUT_INVALID</code></td><td>input that isn&#39;t a valid build shape</td></tr><tr><td><code>KEY_INVALID</code></td><td>syntactically invalid field key</td></tr><tr><td><code>KEY_VALUE_INVALID</code></td><td>value shape doesn&#39;t fit the operator</td></tr><tr><td><code>KEY_UNKNOWN</code></td><td>unrecognized top-level <code>defineQuery</code> input key</td></tr><tr><td><code>KEY_AMBIGUOUS</code></td><td>both <code>sorts</code> and its deprecated alias <code>sort</code> supplied together</td></tr><tr><td><code>OPERATOR_UNSUPPORTED</code></td><td>unknown <code>$</code> operator key</td></tr></tbody></table><h3 id="merge-time-caller-bug" tabindex="-1">Merge time (caller bug) <a class="header-anchor" href="#merge-time-caller-bug" aria-label="Permalink to &quot;Merge time (caller bug)&quot;">​</a></h3><p><code>MergeError</code> with <code>FIELDS_CONDITION_DISCARDED</code>: a fields merge collision would drop a <a href="/guide/fields.html#row-scoped-fields">row-scoped visibility gate</a>. Keep the gated query as the receiver instead of merging it underneath an ungated one.</p><p>Filters have no such code: <a href="/guide/merging-queries.html#filters-monotonic-conjunction"><code>merge()</code></a> is total. It composes every predicate with ordered logical AND, so composition narrows instead of failing.</p><h3 id="parse-time-client-input" tabindex="-1">Parse time (client input) <a class="header-anchor" href="#parse-time-client-input" aria-label="Permalink to &quot;Parse time (client input)&quot;">​</a></h3><p>Most parser parameters throw subclasses of <code>ParseError</code> when <code>throwOnFailure</code> is enabled and otherwise drop disallowed input. Expression filters are stricter: schema-key violations always throw because an expression cannot be partially reinterpreted safely. See <a href="/guide/schemas.html#failure-behavior-drop-vs-throw">drop vs. throw</a>.</p><table tabindex="0"><thead><tr><th>Code</th><th>Trigger</th></tr></thead><tbody><tr><td><code>KEY_NOT_ALLOWED</code></td><td>key outside the schema&#39;s allow-list</td></tr><tr><td><code>KEY_PATH_NOT_ALLOWED</code></td><td>relation path rejected by an allow-list or the relations context</td></tr><tr><td><code>KEY_VALIDATE_REJECTED</code></td><td>key rejected by a schema <a href="/guide/schemas.html#validate-hooks-parse-context"><code>validate</code> hook</a></td></tr><tr><td><code>KEY_COMBINATION_NOT_INDEXED</code></td><td><code>filters</code> or <code>sorts</code> combination an <a href="/guide/schemas.html#indexes"><code>indexed</code> schema</a>&#39;s declared indexes cannot serve</td></tr><tr><td><code>KEY_INVALID</code></td><td>syntactically invalid key under an open schema</td></tr><tr><td><code>KEY_PATH_INVALID</code></td><td>unresolvable relation path</td></tr><tr><td><code>KEY_VALUE_INVALID</code></td><td>malformed value for an operator</td></tr><tr><td><code>KEY_AMBIGUOUS</code></td><td>both <code>sorts</code> and its deprecated alias <code>sort</code> supplied at once</td></tr><tr><td><code>LIMIT_EXCEEDED</code></td><td><code>page[limit]</code> above the schema&#39;s <code>maxLimit</code></td></tr><tr><td><code>OPERATOR_UNSUPPORTED</code></td><td>a recognized dialect operator with no AST counterpart (MongoDB-style parser: known operators like <code>$type</code> / <code>$where</code> / <code>$text</code> / <code>$expr</code>, or <code>$not</code> over a bare <code>$regex</code>); a grammar error that throws regardless of the drop policy</td></tr><tr><td><code>SYNTAX_INVALID</code></td><td>malformed expression / document grammar</td></tr><tr><td><code>INPUT_REJECTED</code></td><td>one or more parts of the input were rejected: the code of every aggregated parse failure, see <a href="#issue-traces">Issue traces</a></td></tr><tr><td><code>INPUT_INVALID</code></td><td>non-object top-level input</td></tr></tbody></table><p>Two dialects are stricter than the drop policy for grammar: <strong>grammar errors always throw</strong>, regardless of schema settings. A malformed expression string (<a href="/packages/parser-expression.html">expression parser</a>) or a broken <code>$</code>-operator document (<a href="/packages/parser-mongo.html">MongoDB-style parser</a>) has no silent-drop reading.</p><p><code>KEY_AMBIGUOUS</code> is unconditional too, on every surface (<code>defineQuery</code>, <code>defineSchema</code>, and every parser/codec <code>parse()</code>/<code>decode()</code>): supplying both <code>sorts</code> and its deprecated alias <code>sort</code> throws regardless of <code>throwOnFailure</code>, since the two spellings name one parameter and picking a winner would silently drop the other. This is a narrower guarantee than <a href="/guide/building-queries.html">unknown keys are rejected</a>: parsers still ignore an unrelated key they don&#39;t recognize; only <code>defineQuery</code> and <code>defineSchema</code> reject those.</p><h3 id="encode-apply-time-query-exceeds-the-target" tabindex="-1">Encode/apply time (query exceeds the target) <a class="header-anchor" href="#encode-apply-time-query-exceeds-the-target" aria-label="Permalink to &quot;Encode/apply time (query exceeds the target)&quot;">​</a></h3><p><code>AdapterError</code>: the query is valid, but the target can&#39;t express it:</p><table tabindex="0"><thead><tr><th>Code</th><th>Trigger</th></tr></thead><tbody><tr><td><code>OPERATOR_UNSUPPORTED</code></td><td>e.g. <code>regex</code>/<code>mod</code>/<code>exists</code>/<code>elemMatch</code> on a URL wire</td></tr><tr><td><code>FEATURE_UNSUPPORTED</code></td><td>e.g. <code>regex</code> on a dialect without regex support; <code>mod</code> on a dialect without a modulo spelling; <code>or(...)</code> over the simple URL dialect; values that wouldn&#39;t survive the wire round trip; a query whose <code>Field</code> carries a <a href="/guide/schemas.html#condition-verdicts">validate-hook condition</a></td></tr><tr><td><code>CONDITION_DETACHED</code></td><td>a condition the built-in consumer cannot lower: either a live custom <code>ICondition</code> that needs a compatible custom adapter/visitor, or detached runtime data from a JSON/RPC/cache round trip. Rebuild detached data with the condition helpers; dropping either would silently widen the result set</td></tr></tbody></table><p>The URL encoders throw these too; a codec never silently changes what a query means. See <a href="/guide/wire.html#what-fits-on-the-wire">What fits on the wire</a>.</p><p>A <code>FEATURE_UNSUPPORTED</code> error from <code>AdapterError.featureUnsupported(...)</code> also carries the refused capability tag (e.g. <code>regexp</code>, <code>filters:mod</code>, <code>filters:regex</code>) as a structured <code>error.feature</code> string, so a capability matrix can be built by reading the property instead of parsing the message:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (e) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (e </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">instanceof</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AdapterError</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> e.code </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ErrorCode.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">FEATURE_UNSUPPORTED</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // e.feature, e.g. &#39;filters:mod&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>error.feature</code> is <code>undefined</code> for every other <code>AdapterError</code> factory (<code>operatorUnsupported</code>, <code>conditionDetached</code>).</p><h3 id="codec-dispatch" tabindex="-1">Codec dispatch <a class="header-anchor" href="#codec-dispatch" aria-label="Permalink to &quot;Codec dispatch&quot;">​</a></h3><p><code>CodecError</code> with <code>CODEC_UNRESOLVABLE</code>: a payload named a codec that isn&#39;t registered. See <a href="/packages/codec-url.html">@rapiq/codec-url</a>.</p><h3 id="schema-registry-server-bug" tabindex="-1">Schema registry (server bug) <a class="header-anchor" href="#schema-registry-server-bug" aria-label="Permalink to &quot;Schema registry (server bug)&quot;">​</a></h3><p><code>SchemaError</code>: the <code>SchemaRegistry</code> was misused or misconfigured. Like <code>BuildError</code>, these indicate a programming error on the receiving side:</p><table tabindex="0"><thead><tr><th>Code</th><th>Trigger</th></tr></thead><tbody><tr><td><code>SCHEMA_NAME_INVALID</code></td><td><code>registry.add()</code> with a schema that has no <code>name</code></td></tr><tr><td><code>SCHEMA_UNRESOLVABLE</code></td><td><code>registry.getOrFail()</code> for a name that isn&#39;t registered</td></tr><tr><td><code>KEY_UNKNOWN</code></td><td><code>defineSchema()</code> with a top-level option key it doesn&#39;t recognize</td></tr><tr><td><code>KEY_AMBIGUOUS</code></td><td><code>defineSchema()</code> with both <code>sorts</code> and its deprecated alias <code>sort</code></td></tr><tr><td><code>SCHEMA_KEY_VALIDATOR_CONFLICT</code></td><td>a <code>fields</code>/<code>relations</code>/<code>sorts</code> sub-schema declares both <a href="/guide/schemas.html#batched-validation-with-validatemany"><code>validate</code> and <code>validateMany</code></a>; thrown while the schema is constructed, since there is no sensible precedence between them</td></tr><tr><td><code>SCHEMA_PRESERVED_CONDITION_PRUNED</code></td><td>the <a href="/guide/relations.html#validate-hooks">relations gate</a> rejected a relation that a <a href="/guide/merging-queries.html#preservation-is-for-relation-pruning"><code>preserve()</code></a> filter condition needs; the two validators contradict each other, see <a href="/guide/recipes/authorization.html#scoping-server-conditions">scoping a filterable field</a></td></tr><tr><td><code>SCHEMA_VALIDATOR_ASYNC_REQUIRES_ASYNC_PARSER</code></td><td><code>parse()</code> (or a synchronous codec method) encountered an async validator (a filter validator or a key validation hook); use the corresponding <code>Async</code> method</td></tr><tr><td><code>SCHEMA_ENTITY_MISMATCH</code></td><td><code>assertSchemaMatchesEntity</code> (<code>@rapiq/adapter-typeorm</code>) found schema keys unknown to the entity; thrown as <code>SchemaEntityMismatchError</code>, which carries the offending <code>schema</code>, <code>entity</code> and <code>keys</code>; see <a href="/packages/adapter-typeorm.html#validating-schemas-against-entities">validating schemas against entities</a></td></tr><tr><td><code>SCHEMA_ENTITY_INDEX_MISMATCH</code></td><td><code>assertSchemaMatchesEntity</code> (<code>@rapiq/adapter-typeorm</code>) found an <a href="/guide/schemas.html#indexes"><code>indexes</code></a> sequence that leads no primary key, unique constraint or index of the entity; thrown as <code>SchemaEntityIndexMismatchError</code>, which carries <code>schema</code>, <code>entity</code> and the offending <code>indexes</code>; see <a href="/packages/adapter-typeorm.html#declared-indexes">declared indexes</a></td></tr></tbody></table><h2 id="issue-traces" tabindex="-1">Issue traces <a class="header-anchor" href="#issue-traces" aria-label="Permalink to &quot;Issue traces&quot;">​</a></h2><p>A request can violate several policies at once, so a parse raises ONE general failure carrying the whole trace of what went wrong on <code>error.issues</code>:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    codec.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">decode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req.query, { schema: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (e) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    e.code;       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;inputRejected&#39;, always</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    e.issues;     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// what was rejected, and where</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Upgrading from 2.0?</p><p>A throwing parse used to fail at the first violation and raise that parameter&#39;s own class. A whole-query parse now aggregates and raises <code>ParseError</code> (<code>inputRejected</code>) instead, so <code>catch (e) { if (e instanceof FieldsParseError) }</code> no longer matches there. Use <a href="#recognizing-an-error"><code>isParseError</code></a> to recognize a client-input failure, and branch on <code>e.issues</code> for what was rejected. Single-parameter parses still raise their parameter&#39;s class.</p></div><p>The code is <code>INPUT_REJECTED</code>, and the class says which parse failed:</p><table tabindex="0"><thead><tr><th>Call</th><th>Raises</th></tr></thead><tbody><tr><td>a whole-query <code>parse()</code> / <code>decode()</code></td><td><code>ParseError</code></td></tr><tr><td>a single-parameter parse (<code>parseFields</code>, <code>SimpleSortsParser.parse</code>, …)</td><td>that parameter&#39;s class (<code>FieldsParseError</code>, <code>SortsParseError</code>, …)</td></tr></tbody></table><p>A single-parameter parse names its parameter because the caller asked about one parameter, so saying which one is the whole truth. A query parse names none: a request can violate policies in four parameters at once, an error advertising one of them would describe a <em>subset</em>, and a consumer branching on that class would act on the part it happened to be handed. The sub-parser failures a query parse catches are merged into its trace rather than raised, so nothing is lost by generalizing: <code>error.issues</code> still says which parameter each rejection came from.</p><p>Issues are plain data, never <code>Error</code> instances: a request may produce many, and only the one error a parse ultimately throws pays for a stack. Every issue is a <strong>failure</strong>: there is no severity, and nothing a parse does without rejecting anything (a substituted default, an entry a rejected relation dragged along) is recorded.</p><p>The nodes are <a href="https://github.com/tada5hi/blemish" target="_blank" rel="noreferrer">blemish</a> issues, the shared issue-tree model, so a rapiq trace crosses library boundaries unchanged: a validator you call inside a <a href="/guide/schemas.html#validate-hooks"><code>validate</code> hook</a> speaks the same shape, and a consumer can nest a rapiq trace inside its own. An issue is one of two node kinds, discriminated by <code>type</code>. Both carry:</p><table tabindex="0"><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>path</code></td><td>canonical, alias-resolved position, leaf included: <code>[&#39;items&#39;, &#39;title&#39;]</code>. Empty for a parameter-level issue</td></tr><tr><td><code>message</code></td><td>human-facing text. <strong>Not</strong> contractual: branch on <code>code</code></td></tr><tr><td><code>meta</code></td><td>provenance the position cannot express, see below</td></tr></tbody></table><p><code>type: &#39;item&#39;</code> is a rejected piece of input, and adds:</p><table tabindex="0"><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>code</code></td><td>the same <code>ErrorCode</code> the error carries: the machine contract</td></tr><tr><td><code>expected</code></td><td>the expected value, when a validator supplies one</td></tr><tr><td><code>received</code></td><td>the offending value, when a value rather than a key was the problem</td></tr></tbody></table><p><code>type: &#39;group&#39;</code> stands for the failures below it, in its own <code>issues</code> array (plus an optional <code>code</code>). Nesting is the whole tree: there is no parent pointer and no depth field.</p><p><code>expected</code> and <code>received</code> are available on the live in-process issue for diagnostics. The default <code>BaseError.toJSON()</code> boundary omits both recursively; serialize a rapiq error directly without assuming raw validation values will cross that boundary.</p><p>rapiq claims two <code>meta</code> keys, both of them facts a consumer cannot reconstruct from the path:</p><table tabindex="0"><thead><tr><th>Key</th><th>Meaning</th></tr></thead><tbody><tr><td><code>meta.parameter</code></td><td>canonical parameter that owns the violated policy (<code>filters</code>, not the wire&#39;s <code>filter</code>). A <code>fields</code> rejection and a <code>filters</code> rejection at <code>[&#39;items&#39;, &#39;secret&#39;]</code> are otherwise indistinguishable</td></tr><tr><td><code>meta.key</code></td><td>raw client spelling at the position that failed. The path is alias-resolved, so the spelling is gone from it</td></tr></tbody></table><p><code>meta</code> is an open bag, so read those through the accessors rather than casting:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { extractIssueKey, extractIssueParameter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">extractIssueParameter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(issue); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;filters&#39; | &#39;fields&#39; | ... | undefined</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">extractIssueKey</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(issue);       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// the raw client spelling, when it differs</span></span></code></pre></div><p>Every node&#39;s <code>path</code> is <strong>absolute</strong>, group included, because merging a nested trace into an enclosing one rewrites the children: a site reports where it failed relative to itself and never has to know the whole position. Recursive validator paths stay absolute through nested filter/simple normalization; <code>ITSELF</code> adds no segment. <code>flattenIssueItems</code> therefore hands you leaves that already know where they sit:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { flattenIssueItems } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;blemish&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">flattenIssueItems</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(e.issues).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">issue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> issue.path);</span></span></code></pre></div><p>The trace has exactly one channel. A parse that raises nothing discards it, so under the default drop policy there is no trace to read: <a href="/guide/schemas.html#failure-behavior-drop-vs-throw"><code>throwOnFailure</code></a> is what turns rejections into something you can inspect.</p><h3 id="the-raise-condition" tabindex="-1">The raise condition <a class="header-anchor" href="#the-raise-condition" aria-label="Permalink to &quot;The raise condition&quot;">​</a></h3><p>Throw mode does not stop at the first violation. The parse records, keeps going across all five parameters, and raises once at the end:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">parser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ fields: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;nope1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], filters: { nope2: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;x&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }, { schema: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ParseError, code &#39;inputRejected&#39;, issues: [fields/nope1, filters/nope2]</span></span></code></pre></div><p><code>issues</code> is an ordinary property, so it shows up when you inspect or spread the error. One consequence worth knowing: deep equality reads enumerable properties, so <code>expect(fn).toThrow(FiltersParseError.keyNotPermitted(&#39;x&#39;))</code> compares the <strong>trace</strong> too. Assert the class, the <code>code</code>, or reach into <code>issues</code>, not whole errors.</p><p>Whether a violation is recorded is decided per site by the policy in effect there, so a dropping <code>fields</code> block and a throwing <code>relations</code> sub-schema coexist in one request: only the latter contributes. A violation takes the policy of the site that found it, which is also what decides what it means: under <code>throwOnFailure</code> a limit above <code>maxLimit</code> is a rejection, not a clamp.</p><p>Every rejection is recorded: allow-list and relation-path rejections, <code>validate</code> hook rejections (fields, sorts, relations <strong>and</strong> filters), unusable filter values, malformed parameter input, pagination values that don&#39;t parse, and <a href="/guide/schemas.html#indexes"><code>indexed</code></a> combinations that fail the parse. <code>MAX_ISSUES</code> counts leaf violations, including leaves nested in groups. Group shape is preserved while an oversized tail is truncated, and ordinary issue leaves preserve encounter order. If a terminal structural abort arrives at a full trace, only the final retained ordinary leaf is displaced so the trace remains bounded and still records why that parameter stopped parsing. The first retained violation is never displaced.</p><p>An issue in one parameter does not suppress another parameter&#39;s independent index check: invalid <code>filters</code> and <code>sorts</code> combinations are both reported. Only a prior failure in the same parameter suppresses its consequence errors, and the index checks judge the tree that would execute: the keys a rejected relation drags along are pruned first, so they never surface as index violations of their own. An explicit empty allowlist denies every supplied key through normal validation; under the established drop policy that remains silent and defaults keep their established behavior. Mongo document grammar errors still throw independently of the policy.</p><p>Structural failures still end their own parameter: a malformed expression string or a <code>$</code>-operator document has no partial reading, so it aborts that parameter and the other four still parse (and still report). <strong>Every</strong> error a parse raises carries its trace, structural aborts included, so <code>error.issues</code> is always the thing to render. The abort becomes an issue like any other rejection: only a rapiq parse error is ever caught (a genuine server bug propagates untouched), and everything a client-input failure knows is in its issue.</p><p>The public expression parser&#39;s <code>parseExact()</code> and <code>parseExactAsync()</code> calls own and finalize an aggregate <code>filters</code> trace too. On failure they raise the aggregate <code>FiltersParseError</code> envelope and expose the underlying validation or syntax violations through <code>error.issues</code>; callers do not need to handle a raw structural error separately.</p><p>A site that fails fast rather than recording (the expression dialect resolves keys under an always-throwing scope, since an expression cannot be partially reinterpreted) attaches the position it failed at to the error it throws. Whoever catches it merges that trace into its own (blemish&#39;s <code>prefixIssuePath</code> is that step), so the rejection reports <code>[&#39;secret&#39;]</code> rather than naming its parameter and nothing else.</p><p>Four consequences worth knowing when you enable <code>throwOnFailure</code>:</p><ul><li>Branch on <code>issues</code>, not on the error class. <code>catch (e) { if (e instanceof FieldsParseError) }</code> no longer matches an aggregated parse; <code>isParseError(e)</code> and <code>e.issues</code> do.</li><li><code>validate</code> hooks now run for keys that the first throw used to shield.</li><li>A filters <code>validate</code> hook that returns <code>undefined</code> now throws <code>KEY_VALIDATE_REJECTED</code>, symmetric with the fields/sorts/relations hooks. A hook that means &quot;drop this quietly&quot; returns a replacement condition instead.</li><li>A <code>ParseError</code> (or subclass) thrown from a <code>validate</code> hook is caught like a structural abort and becomes an issue on the aggregate, carrying the hook&#39;s <code>code</code> and <code>message</code> at the parameter root; the parse still raises <code>inputRejected</code>, whatever the schema&#39;s drop policy says. Any other error a hook throws propagates untouched, as a server bug should. Return <code>undefined</code> (or a replacement condition) to reject a key with its position on the trace.</li></ul><h3 id="formatting-a-trace-for-a-response" tabindex="-1">Formatting a trace for a response <a class="header-anchor" href="#formatting-a-trace-for-a-response" aria-label="Permalink to &quot;Formatting a trace for a response&quot;">​</a></h3><p><code>@rapiq/codec-url</code> normalizes a trace into its response format, mapping the canonical parameter onto the wire vocabulary; only the transport knows that <code>filters</code> reaches the client as <code>filter</code>. The members follow the JSON:API error object rapiq&#39;s query vocabulary is modelled on, so the output drops straight into an <code>errors</code> array:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { formatErrors } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/codec-url&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">formatErrors</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(error.issues, { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;400&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [{</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     status: &#39;400&#39;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     code: &#39;keyNotAllowed&#39;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     detail: &#39;The key secret is not permitted.&#39;,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     source: { parameter: &#39;fields&#39; },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">//     meta: { path: &#39;secret&#39; },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// }]</span></span></code></pre></div><p>It renders the <strong>leaves</strong>: what a group stands for is already said by the issues below it, each at the absolute position merging gave it.</p><h2 id="recognizing-an-error" tabindex="-1">Recognizing an error <a class="header-anchor" href="#recognizing-an-error" aria-label="Permalink to &quot;Recognizing an error&quot;">​</a></h2><p><code>instanceof</code> compares class identity, which two copies of <code>@rapiq/core</code> in one process do not share (mixed ESM/bundled builds, or a dual-packaged dependency). On any boundary a foreign copy could reach, prefer the guards:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { isParseError } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isParseError</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(e)) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // e.code, e.issues</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>isBaseError</code> and <code>isParseError</code> read the <code>@instanceof</code> brand chain every rapiq error carries, so they survive the duplication that <code>instanceof</code> does not, and, because <a href="#crossing-a-boundary"><code>toJSON</code></a> emits the chain, they also recognize an error that arrived as JSON. The brand itself is non-enumerable, so it changes neither deep equality nor what a spread copies. rapiq&#39;s own parsers use these guards internally: a foreign <code>ParseError</code> slipping past an <code>instanceof</code> check would have been rethrown instead of recorded, and the trace would have come back empty.</p><h2 id="crossing-a-boundary" tabindex="-1">Crossing a boundary <a class="header-anchor" href="#crossing-a-boundary" aria-label="Permalink to &quot;Crossing a boundary&quot;">​</a></h2><p><code>JSON.stringify(error)</code> emits a boundary-safe rapiq error, so a failure survives a worker, an SSR hop or a gateway. Its <code>issues</code> retain their structure, but live diagnostic <code>expected</code> and <code>received</code> values are omitted:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;FiltersParseError&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;The input was rejected: 1 violation.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;inputRejected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;issues&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;item&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;keyNotAllowed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;path&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;secret&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;…&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;@instanceof&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@ebec/core/BaseError&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@rapiq/core/error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;@rapiq/core/error/parse&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The <code>@instanceof</code> chain is what makes the guards keep working on the far side: <code>isParseError</code> matches a <strong>plain object</strong> carrying the chain, not just a live error, so a receiver can branch on it without reconstructing anything. <code>instanceof</code> could never answer that question.</p><p>rapiq&#39;s errors extend <a href="https://github.com/tada5hi/ebec" target="_blank" rel="noreferrer"><code>@ebec/core</code></a>&#39;s, the error substrate the other libraries in the family share, which is where the chain, the class name, the stack capture and this payload come from. rapiq adds the one part that is its own: <code>issues</code>. It does <strong>not</strong> use ebec&#39;s group mechanism (<code>errors: Error[]</code>), because everything rapiq aggregates is a rejection, which is data.</p><h2 id="mapping-to-http-responses" tabindex="-1">Mapping to HTTP responses <a class="header-anchor" href="#mapping-to-http-responses" aria-label="Permalink to &quot;Mapping to HTTP responses&quot;">​</a></h2><p>A pragmatic mapping for a typical endpoint:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { isParseError } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { formatErrors } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/codec-url&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">app.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;/users&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">req</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">res</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> query;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        query </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> codec.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">decode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req.query, { schema: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (e) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">isParseError</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(e)) {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // client sent something outside the contract: every violation</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // of the request, not just the first one</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> res.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">json</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">                errors: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">formatErrors</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(e.issues, { status: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;400&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        throw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> e; </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// everything else is a server bug</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">query) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> res.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">400</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">end</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// non-object input</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><ul><li><code>ParseError</code> (with <code>throwOnFailure</code>) → <strong>400</strong>: the client broke the contract; inspect <code>e.issues</code> for each offending key. The aggregate envelope&#39;s <code>e.message</code> does not name a specific key.</li><li><code>BuildError</code> / <code>MergeError</code> / <code>AdapterError</code> / <code>SchemaError</code> on the server → <strong>500</strong>: these mean <em>your</em> code produced or forwarded something invalid.</li><li><code>AdapterError</code> on the caller (encode) → fix the query or switch wire dialect; it never leaves the caller.</li></ul><h2 id="adding-failure-modes-of-your-own" tabindex="-1">Adding failure modes of your own <a class="header-anchor" href="#adding-failure-modes-of-your-own" aria-label="Permalink to &quot;Adding failure modes of your own&quot;">​</a></h2><p>When extending rapiq (custom parsers, adapters), follow the same pattern: add an <code>ErrorCode</code> member and a static factory on the matching error class rather than throwing raw <code>Error</code>s.</p>`,80)])])}const g=s(r,[["render",n]]);export{k as __pageData,g as default};
