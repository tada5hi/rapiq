import{_ as s,o as t,c as i,a2 as a}from"./chunks/framework.CpXckY1Z.js";const k=JSON.parse('{"title":"Filters","description":"","frontmatter":{},"headers":[],"relativePath":"guide/filters.md","filePath":"guide/filters.md","lastUpdated":1784233504000}'),d={name:"guide/filters.md"};function n(o,e,l,r,c,h){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="filters" tabindex="-1">Filters <a class="header-anchor" href="#filters" aria-label="Permalink to &quot;Filters&quot;">​</a></h1><p>Narrow the collection by conditions on fields — from a simple equality to arbitrary <code>and</code>/<code>or</code> trees.</p><table tabindex="0"><thead><tr><th></th><th></th></tr></thead><tbody><tr><td>URL key</td><td><code>filter</code></td></tr><tr><td>AST nodes</td><td><code>Filters</code> (compound <code>and</code>/<code>or</code>) / <code>Filter { operator, field, value }</code></td></tr><tr><td>Schema options</td><td><code>allowed</code>, <code>default</code>, <code>mapping</code>, <code>validate</code></td></tr></tbody></table><h2 id="operators" tabindex="-1">Operators <a class="header-anchor" href="#operators" aria-label="Permalink to &quot;Operators&quot;">​</a></h2><p>Every dialect maps onto the same operator set (<code>FilterFieldOperator</code>):</p><table tabindex="0"><thead><tr><th>Operator</th><th>Meaning</th><th>Build key</th><th>Helper</th><th>Simple wire</th></tr></thead><tbody><tr><td><code>EQUAL</code></td><td>equals</td><td><code>$eq</code> / bare scalar</td><td><code>eq</code></td><td><code>admin</code>, <code>1</code>, <code>null</code></td></tr><tr><td><code>NOT_EQUAL</code></td><td>not equals</td><td><code>$ne</code></td><td><code>ne</code></td><td><code>!admin</code></td></tr><tr><td><code>LESS_THAN</code></td><td><code>&lt;</code></td><td><code>$lt</code></td><td><code>lt</code></td><td><code>&lt;18</code></td></tr><tr><td><code>LESS_THAN_EQUAL</code></td><td><code>&lt;=</code></td><td><code>$lte</code></td><td><code>lte</code></td><td><code>&lt;=18</code></td></tr><tr><td><code>GREATER_THAN</code></td><td><code>&gt;</code></td><td><code>$gt</code></td><td><code>gt</code></td><td><code>&gt;18</code></td></tr><tr><td><code>GREATER_THAN_EQUAL</code></td><td><code>&gt;=</code></td><td><code>$gte</code></td><td><code>gte</code></td><td><code>&gt;=18</code></td></tr><tr><td><code>IN</code></td><td>in list</td><td><code>$in</code> / bare array</td><td><code>inArray</code></td><td><code>1,2,3</code></td></tr><tr><td><code>NOT_IN</code></td><td>not in list</td><td><code>$nin</code></td><td><code>nin</code></td><td><code>!1,2,3</code></td></tr><tr><td><code>STARTS_WITH</code></td><td>prefix</td><td><code>$startsWith</code></td><td><code>startsWith</code></td><td><code>jo~</code></td></tr><tr><td><code>NOT_STARTS_WITH</code></td><td>not prefix</td><td><code>$notStartsWith</code></td><td><code>notStartsWith</code></td><td><code>!jo~</code></td></tr><tr><td><code>ENDS_WITH</code></td><td>suffix</td><td><code>$endsWith</code></td><td><code>endsWith</code></td><td><code>~jo</code></td></tr><tr><td><code>NOT_ENDS_WITH</code></td><td>not suffix</td><td><code>$notEndsWith</code></td><td><code>notEndsWith</code></td><td><code>!~jo</code></td></tr><tr><td><code>CONTAINS</code></td><td>substring</td><td><code>$contains</code></td><td><code>contains</code></td><td><code>~jo~</code></td></tr><tr><td><code>NOT_CONTAINS</code></td><td>not substring</td><td><code>$notContains</code></td><td><code>notContains</code></td><td><code>!~jo~</code></td></tr><tr><td><code>REGEX</code></td><td>pattern</td><td><code>$regex</code></td><td><code>regex</code></td><td>—</td></tr><tr><td><code>MOD</code></td><td>divisible remainder</td><td><code>$mod</code></td><td><code>mod</code></td><td>—</td></tr><tr><td><code>EXISTS</code></td><td>is not null</td><td><code>$exists</code></td><td><code>exists</code></td><td>—</td></tr><tr><td><code>ELEM_MATCH</code></td><td>array element match</td><td><code>$elemMatch</code></td><td><code>elemMatch</code></td><td>—</td></tr></tbody></table><p>The last four have no representation in the URL dialects — they work in code, via the <a href="/packages/parser-mongo.html">MongoDB-style parser</a>, and in every <a href="/guide/executing-queries.html">adapter</a>.</p><h2 id="on-the-wire-expression-dialect" tabindex="-1">On the wire (expression dialect) <a class="header-anchor" href="#on-the-wire-expression-dialect" aria-label="Permalink to &quot;On the wire (expression dialect)&quot;">​</a></h2><p>The URL codec writes one expression by default, preserving compound structure and repeated fields:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>codec=url-expression&amp;filter=and(gte(age,&#39;18&#39;),or(eq(status,&#39;active&#39;),eq(status,&#39;pending&#39;)))</span></span></code></pre></div><p>The function names mirror the condition helpers in the table above. Values are single-quoted; quote characters escape by doubling (<code>&#39;it&#39;&#39;s&#39;</code>).</p><h2 id="legacy-simple-input" tabindex="-1">Legacy simple input <a class="header-anchor" href="#legacy-simple-input" aria-label="Permalink to &quot;Legacy simple input&quot;">​</a></h2><p>Keys are field names (or <code>relation.field</code> paths); the value string encodes the operator:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>filter[id]=1                  equal</span></span>
<span class="line"><span>filter[name]=~jo~             contains</span></span>
<span class="line"><span>filter[age]=&gt;=18              greater than or equal</span></span>
<span class="line"><span>filter[realm.name]=master     nested field</span></span>
<span class="line"><span>filter[id]=1,2,3              in list</span></span>
<span class="line"><span>filter[email]=!null           not null</span></span></code></pre></div><p>The v2 codec continues to decode this shape for existing clients. Multiple keys combine with <strong>and</strong>. Scalar coercion applies on decode: <code>&#39;18&#39;</code> → <code>18</code>, <code>&#39;true&#39;</code> → <code>true</code>, <code>&#39;null&#39;</code> → <code>null</code>.</p><div class="info custom-block"><p class="custom-block-title">Wire strings vs. code</p><p>The <code>!</code>/<code>&lt;</code>/<code>~</code> prefixes are the <em>wire</em> format. In code, use typed operator objects (<code>{ age: { $gte: 18 } }</code>) or condition helpers — never magic strings.</p></div><h2 id="building-in-code" tabindex="-1">Building in code <a class="header-anchor" href="#building-in-code" aria-label="Permalink to &quot;Building in code&quot;">​</a></h2><p>The full grammar is covered in <a href="/guide/building-queries.html">Building Queries</a>; the short version:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { and, defineQuery, eq, gte, or } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;John&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// scalar → eq</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &#39;realm.id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// array → in (null is legal)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        age: { $gte: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, $lt: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">65</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// operator object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">or</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gte</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;age&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">eq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;deleted_at&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)),   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// condition trees</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><h2 id="or-compound-trees" tabindex="-1">OR &amp; compound trees <a class="header-anchor" href="#or-compound-trees" aria-label="Permalink to &quot;OR &amp; compound trees&quot;">​</a></h2><p>The simple object/wire dialect only expresses flat <strong>and</strong> sets. For <code>or</code> and nested groups:</p><ul><li><strong>in code</strong> — condition helpers: <code>or(...)</code>, <code>and(...)</code>, arbitrarily nested;</li><li><strong>over a URL</strong> — the <a href="/packages/codec-url.html#expression-dialect">expression codec</a>: <code>filter=or(gte(age,&#39;18&#39;),eq(status,&#39;active&#39;))</code>;</li><li><strong>in a request body</strong> — the <a href="/packages/parser-mongo.html">MongoDB-style parser</a>: <code>{ $or: [...] }</code>.</li></ul><h2 id="nested-fields" tabindex="-1">Nested fields <a class="header-anchor" href="#nested-fields" aria-label="Permalink to &quot;Nested fields&quot;">​</a></h2><p><code>relation.field</code> keys filter on related records. The relation must be permitted and requested via <a href="/guide/relations.html">relations</a>, and the field validates against the related schema through <a href="/guide/schemas.html#the-registry--relations"><code>schemaMapping</code></a>.</p><h2 id="null-semantics" tabindex="-1">Null semantics <a class="header-anchor" href="#null-semantics" aria-label="Permalink to &quot;Null semantics&quot;">​</a></h2><p><code>null</code> is a first-class filter value with SQL-consistent behavior across all adapters:</p><table tabindex="0"><thead><tr><th>Filter</th><th>Meaning</th></tr></thead><tbody><tr><td><code>eq(field, null)</code></td><td><code>field IS NULL</code></td></tr><tr><td><code>ne(field, null)</code></td><td><code>field IS NOT NULL</code></td></tr><tr><td><code>inArray(field, [a, null])</code></td><td><code>field IN (…) OR field IS NULL</code></td></tr><tr><td><code>exists(field)</code></td><td><code>field IS NOT NULL</code></td></tr></tbody></table><p>Negated operators (<code>ne</code>, <code>nin</code>, <code>notContains</code>, <code>notStartsWith</code>, <code>notEndsWith</code>) are <strong>exact complements</strong> of their positive twins — they also match records where the field is <code>NULL</code>/absent. Adapters render them null-inclusively, e.g. <code>ne(field, a)</code> → <code>(field &lt;&gt; ? OR field IS NULL)</code>.</p><h2 id="case-sensitivity" tabindex="-1">Case sensitivity <a class="header-anchor" href="#case-sensitivity" aria-label="Permalink to &quot;Case sensitivity&quot;">​</a></h2><p>String matching is <strong>case-insensitive by default</strong>, uniformly across every adapter — the same query matches the same records whether it runs on Postgres, MySQL, in memory, or through TypeORM:</p><ul><li>The <strong>equality family</strong> (<code>eq</code>, <code>ne</code>, <code>in</code>, <code>nin</code>) compares string values case-insensitively: <code>eq(&#39;name&#39;, &#39;super hero&#39;)</code> matches <code>Super Hero</code>. Non-string values (numbers, booleans, dates, <code>null</code>) are unaffected.</li><li>The <strong>anchored operators</strong> (<code>contains</code>, <code>startsWith</code>, <code>endsWith</code> and their negations) match case-insensitively as well.</li><li><strong>Range comparisons</strong> (<code>lt</code>/<code>lte</code>/<code>gt</code>/<code>gte</code>) and <code>sort</code> follow the backend&#39;s collation — rapiq does not fold string ordering.</li></ul><p>Opt out per field with the <code>caseSensitive</code> schema option where exactness matters — identifiers, tokens, enum-like codes:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> schema</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        caseSensitive: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The receiving side forwards the list to its adapter:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// @rapiq/sql &amp; @rapiq/typeorm</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, { visitor: { caseSensitive: schema.filters.caseSensitive } });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// @rapiq/memory</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">applyQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, data, { filters: { caseSensitive: schema.filters.caseSensitive } });</span></span></code></pre></div><p>Under the hood, <code>@rapiq/sql</code> folds both sides of the comparison — <code>lower(field) = lower(?)</code> — and only when the filter value is a string. Dialects whose plain <code>=</code> already compares case-insensitively under their default collation (the MySQL and MSSQL presets) skip the folding through the <code>caseFold</code> dialect option, so plain indexes stay usable. On folding dialects (Postgres, SQLite, Oracle), add an expression index for hot string filter columns — <code>CREATE INDEX ON &quot;user&quot; (lower(name))</code> — or list the column in <code>caseSensitive</code>.</p><p>The TypeORM adapter goes one step further: it resolves each filtered field against the entity metadata and folds <strong>only string-typed columns</strong>. Numeric, date, uuid or enum columns never pay the <code>lower()</code> cost — even when the value arrives as an untyped wire string like <code>filter[age]=18</code>.</p><div class="warning custom-block"><p class="custom-block-title">Collation wins on MySQL/MSSQL</p><p>On the MySQL/MSSQL presets, equality delegates to the column collation: <code>caseSensitive</code> cannot force exactness onto a <code>*_ci</code> collated column. Use a <code>*_bin</code>/<code>*_cs</code> collation for such columns, or override <code>caseFold</code> with a <code>lower()</code>-wrapping implementation.</p></div><h2 id="schema-options" tabindex="-1">Schema options <a class="header-anchor" href="#schema-options" aria-label="Permalink to &quot;Schema options&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;age&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        mapping: { aliasId: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        default: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">eq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;status&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;active&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // accept (return the filter), replace (return another Filter),</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // or reject (return undefined) — a hook that returns nothing</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // rejects every filter.</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">        validate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">async</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> filter,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Option</th><th>Description</th></tr></thead><tbody><tr><td><code>allowed</code></td><td>Filterable field names. Omit to allow all; <code>[]</code> blocks the parameter.</td></tr><tr><td><code>default</code></td><td>Condition applied when the client sends no filters.</td></tr><tr><td><code>mapping</code></td><td>Alias → field translation applied before validation.</td></tr><tr><td><code>validate</code></td><td>Sync or async per-filter hook — inspect/replace a parsed <code>Filter</code>, or reject it.</td></tr><tr><td><code>caseSensitive</code></td><td>Fields whose equality comparisons stay exact instead of the <a href="#case-sensitivity">case-insensitive default</a>.</td></tr></tbody></table><p><code>validate</code> runs after key resolution, mapping and value coercion. Return the original filter to accept it, another <code>Filter</code> to replace it, or <code>undefined</code> to reject that leaf — an inspect-only hook must still <code>return</code> the filter, otherwise every leaf is rejected. <code>$elemMatch</code> conditions are validated inside-out: every interior leaf passes the hook, then the <code>elemMatch</code> filter itself. The return value may also be a Promise of any of those results. On the server, <a href="/packages/codec-url.html">schema-aware encoding</a> re-runs the schema-bound decoder, so a validator that is not idempotent (e.g. one that appends to the value) transforms a filter twice between a schema-aware <code>encode()</code> and the receiving <code>decode()</code> — keep validators idempotent.</p><p>Use the synchronous <code>parse()</code> / <code>decode()</code> / schema-aware <code>encode()</code> methods when every validator is synchronous. Use their <code>Async</code> counterparts when a validator may be asynchronous:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> parser.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(input, { schema });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> decoded</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> codec.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">decodeAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req.query, { schema });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> encoded</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> codec.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">encodeAsync</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, { schema });</span></span></code></pre></div><p>The async path awaits validators sequentially in filter-tree order. Calling a synchronous method when a validator returns a Promise/thenable throws a <code>SchemaError</code> with <code>SCHEMA_VALIDATOR_ASYNC_REQUIRES_ASYNC_PARSER</code>. Compound <code>and</code>/<code>or</code> structure is preserved; if every submitted leaf is rejected, the schema default is applied.</p><h2 id="on-violation" tabindex="-1">On violation <a class="header-anchor" href="#on-violation" aria-label="Permalink to &quot;On violation&quot;">​</a></h2><p>Legacy simple and MongoDB field-key failures follow the schema&#39;s drop-vs-throw policy. Expression filters are precise: syntax and schema-key violations throw <code>FiltersParseError</code>. See <a href="/guide/errors.html">Error Handling</a>.</p>`,47)])])}const g=s(d,[["render",n]]);export{k as __pageData,g as default};
