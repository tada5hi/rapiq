import{_ as e,o as a,c as i,a2 as t}from"./chunks/framework.CpXckY1Z.js";const k=JSON.parse('{"title":"Grouping & Aggregates","description":"","frontmatter":{},"headers":[],"relativePath":"guide/grouping.md","filePath":"guide/grouping.md","lastUpdated":1790157319000}'),n={name:"guide/grouping.md"};function o(d,s,r,l,h,p){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="grouping-aggregates" tabindex="-1">Grouping &amp; Aggregates <a class="header-anchor" href="#grouping-aggregates" aria-label="Permalink to &quot;Grouping &amp; Aggregates&quot;">​</a></h1><p>Turn a collection read into grouped rows: a time series, a breakdown by status, a count per scope. Two parameters work together: <code>groups</code> names the dimensions a row is keyed by, <code>aggregates</code> names the measures computed per row.</p><table tabindex="0"><thead><tr><th></th><th>Groups</th><th>Aggregates</th></tr></thead><tbody><tr><td>URL key</td><td><code>group</code></td><td><code>aggregate</code></td></tr><tr><td>AST nodes</td><td><code>Groups</code> / <code>Group { key, name, params, lowering }</code></td><td><code>Aggregates</code> / <code>Aggregate { key, name, params, lowering }</code></td></tr><tr><td>Schema options</td><td><code>allowed</code>, <code>functions</code></td><td><code>functions</code></td></tr><tr><td>Built-in functions</td><td><code>bucket(field, unit)</code></td><td><code>count()</code>, <code>count(field)</code>, <code>sum(field)</code></td></tr></tbody></table><p>A query that carries neither parameter is the same query as before; nothing changes for existing reads.</p><h2 id="on-the-wire" tabindex="-1">On the wire <a class="header-anchor" href="#on-the-wire" aria-label="Permalink to &quot;On the wire {#on-the-wire}&quot;">​</a></h2><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>group=scope,name                               bare columns</span></span>
<span class="line"><span>group=bucket(createdAt,day),scope,name         a UTC day bucket plus two columns</span></span>
<span class="line"><span>aggregate=count                                rows per group (count() is the same)</span></span>
<span class="line"><span>aggregate=count(couponId),sum(amount)          non-null couponId values, sum of amount</span></span>
<span class="line"><span>group=scope&amp;group=bucket(createdAt,day)        repeated keys are concatenated</span></span></code></pre></div><p>A typical dashboard request:</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET /events?filter[realmId]=&lt;id&gt;&amp;group=bucket(createdAt,day),scope,name&amp;aggregate=count</span></span></code></pre></div><p>answers rows shaped <code>{ bucket: &#39;2026-09-22T00:00:00.000Z&#39;, scope, name, count: 3 }</code>.</p><p>The grammar is the same in every parser dialect (simple, expression, MongoDB-style):</p><ul><li>a comma separates terms only outside parentheses, so <code>bucket(createdAt,day),scope</code> is two terms;</li><li>a term is an identifier, optionally followed by an argument list <code>name(a,b)</code>; <code>count</code> and <code>count()</code> are equal;</li><li>spaces and tabs around identifiers are trimmed: <code>bucket(createdAt, day)</code> is accepted;</li><li>an empty term (<code>a,,b</code>, a trailing comma), unbalanced or nested parentheses, text after <code>)</code>, an empty argument (<code>f(a,)</code>) and quote characters are grammar errors (<code>SYNTAX_INVALID</code>). Quotes are reserved for a future time-zone argument.</li></ul><p><code>encodeURIComponent</code> escapes the comma and keeps parentheses literal, so a browser-built URL decodes unchanged.</p><h2 id="opting-in" tabindex="-1">Opting in <a class="header-anchor" href="#opting-in" aria-label="Permalink to &quot;Opting in {#opting-in}&quot;">​</a></h2><p>Both parameters are <strong>off by default</strong>. A server parses them only when it asks for them, either by listing them in <code>parameters</code> or by flagging them <code>true</code>:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createURLCodec } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/codec-url&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> codec</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createURLCodec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(registry);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> codec.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">decode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req.query, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    schema: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;event&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    parameters: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;filters&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;groups&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;aggregates&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sorts&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pagination&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// or keep every other parameter and opt in to these two</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">codec.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">decode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req.query, { schema: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;event&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, groups: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, aggregates: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre></div><p>An endpoint that does not opt in ignores <code>group=</code> and <code>aggregate=</code> exactly as it ignored any unknown key before, so upgrading rapiq never turns an entity list into grouped rows behind your back. A <code>parameters</code> list that omits <code>groups</code> skips them even when <code>groups: true</code> is set, and <code>groups: false</code> skips them even when they are listed. The same options apply to <code>parser.parse()</code> / <code>parseAsync()</code> on every parser.</p><h2 id="declaring" tabindex="-1">Declaring what may be grouped <a class="header-anchor" href="#declaring" aria-label="Permalink to &quot;Declaring what may be grouped {#declaring}&quot;">​</a></h2><p>A schema lists which columns may be grouped and which functions may be called on which columns:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineSchema } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Event</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;event&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: { allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;realmId&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;scope&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;createdAt&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    groups: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;scope&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        functions: { bucket: { allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;createdAt&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    aggregates: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        functions: { count: {} },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Option</th><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>allowed</code></td><td><code>groups</code></td><td>Root columns a client may group by as bare keys (<code>group=scope</code>).</td></tr><tr><td><code>functions.bucket.allowed</code></td><td><code>groups</code></td><td>Temporal columns <code>bucket(&lt;column&gt;, hour|day|month)</code> may be called on.</td></tr><tr><td><code>functions.count.allowed</code></td><td><code>aggregates</code></td><td>Columns <code>count(&lt;column&gt;)</code> may be called on. <code>count()</code> (row count) is always available once <code>count</code> is declared, so <code>count: {}</code> permits exactly <code>count</code>.</td></tr><tr><td><code>functions.sum.allowed</code></td><td><code>aggregates</code></td><td>Numeric columns <code>sum(&lt;column&gt;)</code> may be called on.</td></tr><tr><td><code>functions.&lt;name&gt;</code></td><td>both</td><td>A <a href="#named-functions">named function</a> binding one of the built-ins.</td></tr></tbody></table><p><strong>Grouping is a disclosure surface of its own.</strong> A group by <code>actorName</code> enumerates every distinct value of a column that a reader may otherwise only filter by. The allow-list is therefore declared explicitly per parameter and never derived from <code>filters.allowed</code>.</p><p><strong>Fail-closed.</strong> With a bound schema, a missing <code>groups</code> or <code>aggregates</code> block permits nothing: every term is rejected with <code>KEY_NOT_ALLOWED</code>. This is the opposite of the other parameters, where an undeclared allow-list falls back to a syntactic check. <code>strict</code> and <code>throwOnFailure</code> do not apply to these two parameters: every rejection fails the parse (see <a href="#errors">Errors</a>).</p><p><strong>Schemaless parsing</strong> (no <code>schema</code> option) accepts the built-in functions and bare columns with a valid identifier. Named functions need a schema.</p><p>Only root columns can be grouped or aggregated: a dotted key (<code>realm.name</code>) is rejected with <code>KEY_PATH_NOT_ALLOWED</code>. Bucket units are <code>hour</code>, <code>day</code> and <code>month</code>.</p><h2 id="named-functions" tabindex="-1">Named functions <a class="header-anchor" href="#named-functions" aria-label="Permalink to &quot;Named functions {#named-functions}&quot;">​</a></h2><p>A schema can also declare a named function that binds a built-in. Every argument slot of the built-in is either <strong>fixed</strong> by the schema (a scalar) or <strong>open</strong> to the client (an array of permitted values):</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Order</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;order&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    groups: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;status&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        functions: {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // wire: period(hour) or period(day); the column is fixed</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            period: { fn: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;bucket&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, field: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;createdAt&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, unit: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;hour&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;day&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    aggregates: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        functions: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            count: { allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;couponId&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // wire: total(amount) or total(fee)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            total: { fn: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sum&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, field: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;amount&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;fee&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // wire: revenue; both the function and its column are fixed</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            revenue: { fn: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sum&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, field: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;amount&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The client passes only the open slots, in the built-in&#39;s argument order. A named function hides the column name from the wire (<code>period(day)</code> never mentions <code>createdAt</code>) and narrows the units or columns without a separate option. Lowering stays built-in: a named function can only bind <code>bucket</code>, <code>count</code> or <code>sum</code>, never custom SQL.</p><p>Declaration mistakes throw <code>SchemaError</code> (<code>KEY_INVALID</code>) when the schema is defined: an unknown <code>fn</code>, a missing or unknown slot, an empty open slot, an invalid unit or column name, a built-in key carrying anything but <code>allowed</code>, or a group function named like an allowed column (it would shadow it).</p><h2 id="output-keys" tabindex="-1">Output keys <a class="header-anchor" href="#output-keys" aria-label="Permalink to &quot;Output keys {#output-keys}&quot;">​</a></h2><p>Every group and aggregate becomes one key of each result row. The key is derived from the wire form alone, so client and server compute the same key and no <code>as</code> syntax is needed:</p><ul><li>a group&#39;s key is its name: <code>scope</code>, <code>bucket</code>, <code>period</code>;</li><li>an aggregate&#39;s key is its name joined with its arguments by <code>_</code>: <code>count</code>, <code>count_couponId</code>, <code>sum_amount</code>.</li></ul><table tabindex="0"><thead><tr><th>Request</th><th>Row keys</th></tr></thead><tbody><tr><td><code>group=scope,name</code></td><td><code>scope</code>, <code>name</code></td></tr><tr><td><code>group=bucket(createdAt,day)</code></td><td><code>bucket</code></td></tr><tr><td><code>group=period(day)</code> (named)</td><td><code>period</code></td></tr><tr><td><code>aggregate=count</code> or <code>aggregate=count()</code></td><td><code>count</code></td></tr><tr><td><code>aggregate=count(couponId)</code></td><td><code>count_couponId</code></td></tr><tr><td><code>aggregate=sum(amount),sum(fee)</code></td><td><code>sum_amount</code>, <code>sum_fee</code></td></tr><tr><td><code>aggregate=total(amount),total(fee)</code> (named, open column)</td><td><code>total_amount</code>, <code>total_fee</code></td></tr><tr><td><code>aggregate=revenue</code> (named, fixed column)</td><td><code>revenue</code></td></tr><tr><td><code>group=bucket(createdAt,day),bucket(createdAt,hour)</code></td><td>rejected: <code>KEY_AMBIGUOUS</code></td></tr><tr><td><code>group=count&amp;aggregate=count</code></td><td>rejected: <code>KEY_AMBIGUOUS</code></td></tr><tr><td><code>aggregate=count,count()</code></td><td>rejected: <code>KEY_AMBIGUOUS</code></td></tr></tbody></table><p>Groups keep the bare name because two groups with the same callee are two grains of one dimension, a real collision; declare a named function per grain if a query needs both.</p><p>Keys become SQL column aliases, so they are bound by the engine&#39;s identifier limit: 63 bytes on PostgreSQL (a longer alias is truncated), 30 on Oracle before 12.2. <code>normalizeGroupedRows</code> (and <code>normalize</code>) refuse a driver row missing an output key with <code>KEY_VALUE_INVALID</code> rather than reading it as <code>null</code>, so keep column and function names short enough that <code>sum_&lt;column&gt;</code> fits.</p><h2 id="grouped-mode" tabindex="-1">Grouped mode <a class="header-anchor" href="#grouped-mode" aria-label="Permalink to &quot;Grouped mode {#grouped-mode}&quot;">​</a></h2><p>Once a query carries a group or an aggregate, the other parameters change meaning:</p><ul><li><strong>Filters</strong> select the rows before they are grouped (<code>WHERE</code>). There is no filter on the aggregated values (no <code>HAVING</code>).</li><li><strong>Fields</strong> are refused: a client <code>fields</code> input fails the parse with <code>FEATURE_UNSUPPORTED</code>, and a schema&#39;s <code>fields.default</code> is not applied. The row shape is the output keys.</li><li><strong>Relations</strong> are still parsed, because they gate which relation paths a filter may traverse. They are never hydrated into the rows, and the grouped entry points join only the relations a filter traverses.</li><li><strong>Sorts</strong> may name output keys only: <code>sort=-count</code> or <code>sort=bucket</code> are accepted, <code>sort=age</code> and <code>sort=realm.name</code> are rejected under the usual <a href="/guide/sort.html#on-violation">sorts policy</a>. The schema&#39;s <code>sorts.default</code>, <code>sorts.validate</code> and <code>sorts.indexed</code> do not apply. Without a sort, rows are ordered by every group key ascending, in declared order.</li><li><strong>Pagination</strong> limits group rows, and <code>pagination.maxLimit</code> caps them. A dashboard asking for 744 hourly buckets times several scopes can exceed a typical <code>maxLimit</code>. The SQL and TypeORM adapters run no separate count query, so <strong><code>rows.length === limit</code> is the signal that the series may be truncated</strong>; ask for the next page (<code>offset</code>) or narrow the window. The memory adapter additionally returns <code>total</code>.</li></ul><p>An aggregates-only query (<code>aggregate=count</code> without <code>group</code>) answers exactly one row. Groups over zero matching rows answer zero rows.</p><p><code>null</code> (and, in memory, a missing value) is a group of its own. <strong>Where the <code>null</code> group lands in the order differs by engine and is not normalized:</strong> PostgreSQL and the memory adapter sort it last ascending (first descending), MySQL and SQLite first ascending. A client that needs a stable position should not rely on it, or should filter the <code>null</code> rows out (<code>exists(scope)</code>, or <code>filter[scope]=!null</code> in the legacy dialect).</p><h2 id="buckets" tabindex="-1">Buckets are UTC <a class="header-anchor" href="#buckets" aria-label="Permalink to &quot;Buckets are UTC {#buckets}&quot;">​</a></h2><p><code>bucket(column, unit)</code> truncates to the start of the UTC hour, day or month and is returned as ISO text, <code>YYYY-MM-DDTHH:MM:SS.000Z</code>, byte-identical to <code>Date#toISOString()</code>. The text is produced by the database, never by the driver, so the answer does not depend on the host&#39;s time zone. The unit is inlined from a closed list and never bound as a parameter.</p><p>Know your column:</p><ul><li><strong>PostgreSQL <code>timestamptz</code> through <code>@rapiq/adapter-sql</code></strong>: standalone SQL has no column metadata and treats every column as a zone-less <code>datetime</code>, which PostgreSQL would truncate in the session time zone. Override <code>temporalKind(field)</code> on the filters adapter to return <code>&#39;instant&#39;</code> for zoned columns (see <a href="/packages/adapter-sql.html#grouped-queries">@rapiq/adapter-sql</a>). <code>@rapiq/adapter-typeorm</code> reads the entity metadata and needs nothing.</li><li><strong>MySQL <code>TIMESTAMP</code></strong>: MySQL converts it to the session time zone before formatting, so it buckets in UTC only when the session <code>time_zone</code> is <code>&#39;+00:00&#39;</code>. Set it for every connection (<code>SET time_zone = &#39;+00:00&#39;</code>, or the server&#39;s <code>default-time-zone</code>). <code>DATETIME</code> columns are read as stored.</li><li><strong>SQLite</strong>: the bucket expects ISO text or <code>YYYY-MM-DD HH:MM:SS</code> text, the form TypeORM writes. Epoch numbers are not recognized (SQLite reads a number as a Julian day).</li><li><strong><code>hour</code> on a <code>date</code> column</strong> answers midnight buckets, the same on every backend.</li><li><strong>Prefer <code>bucket(...)</code> over a bare group on a temporal column.</strong> A bare group returns the driver&#39;s value: node-postgres reads a zone-less <code>timestamp</code> as a <code>Date</code> in the host&#39;s time zone, so the same row reads as a different instant on a UTC+2 host than on a UTC one, while the memory adapter returns the stored value unchanged. Bucket text is computed in SQL and does not depend on the host.</li><li><strong>MySQL collations</strong>: a <code>*_ci</code> collation groups case variants of a bare column (<code>Login</code>, <code>login</code>) into one row. The memory adapter, PostgreSQL and SQLite keep them apart.</li></ul><p>Empty buckets are not returned: see <a href="#zero-fill">zero-filling</a>.</p><h2 id="numbers" tabindex="-1">Numbers <a class="header-anchor" href="#numbers" aria-label="Permalink to &quot;Numbers {#numbers}&quot;">​</a></h2><p><code>count</code> and <code>sum</code> are returned as JavaScript numbers on every adapter (PostgreSQL returns a string for both, MySQL a DECIMAL string for <code>sum</code>, SQLite a number). Through TypeORM on MySQL, <code>count</code> comes back as a string too. The SQL and TypeORM adapters convert them with <code>Number</code> in <code>normalizeGroupedRows</code> and <code>normalize</code>. A <code>sum</code> over no non-null value is <code>null</code>. The price is precision: a sum beyond <code>2^53</code> (about 15 significant digits) or an exact decimal such as a money column may round. If you need exact decimals, render the SQL with <code>executeGrouped</code> and read the driver values yourself instead of calling <code>normalizeGroupedRows</code> or <code>normalize</code>.</p><p>The memory adapter adds JavaScript numbers, so a decimal sum carries float error there: <code>1.1 + 2.2</code> is <code>3.3000000000000003</code>. PostgreSQL and MySQL add a <code>decimal</code> column exactly (<code>3.30</code>), so compare decimal sums from memory and from the database with a tolerance, not with strict equality.</p><p><code>sum</code> adds finite numbers only in memory; a column holding text is an application error, not a value. The engines disagree on it: the memory adapter skips a numeric string such as <code>&#39;5&#39;</code>, while SQLite and MySQL coerce text to a number and add it (PostgreSQL refuses <code>sum</code> over a text column).</p><h2 id="building" tabindex="-1">Building in code <a class="header-anchor" href="#building" aria-label="Permalink to &quot;Building in code {#building}&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { defineQuery } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    groups: [{ name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;bucket&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, params: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;createdAt&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;day&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;scope&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;name&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    aggregates: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;count&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    sorts: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;-count&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>A string is a term without arguments; <code>{ name, params }</code> is a call. Built-ins and bare group columns are resolved on the spot. A call to a name the build layer does not know (<code>{ name: &#39;period&#39;, params: [&#39;day&#39;] }</code>, or the aggregate <code>&#39;revenue&#39;</code>) is kept as an unresolved node: it encodes to <code>period(day)</code> for a server that declares <code>period</code>, but no adapter executes it. <code>BuildError</code> reports an invalid identifier (<code>KEY_INVALID</code>), bad arguments (<code>KEY_VALUE_INVALID</code>) and a duplicate output key (<code>KEY_AMBIGUOUS</code>).</p><p><code>createURLCodec().encode(query)</code> writes <code>group=</code> and <code>aggregate=</code> after every other parameter, so the encoding of a query without groups is unchanged. A schema-aware encode validates them like the server would.</p><h2 id="merging" tabindex="-1">Merging <a class="header-anchor" href="#merging" aria-label="Permalink to &quot;Merging {#merging}&quot;">​</a></h2><p><code>mergeQueries</code> accepts groups or aggregates from one side only, or identical ones from both. Two different non-empty definitions throw <code>MergeError</code> (<code>KEY_AMBIGUOUS</code>): a union or a left win would fabricate or drop a grain. &quot;Identical&quot; compares the resolved lowering too, so a term resolved by a parse and the same wire form built unresolved on the client (<code>period(day)</code> from <code>defineQuery</code>) are <strong>not</strong> identical and are refused as well: merge the parsed query with filters or pagination, not with a second copy of its groups. Filters keep merging as a conjunction, so a server-side reach condition appended with <code>filters.and()</code> composes with a grouped query as usual.</p><h2 id="executing" tabindex="-1">Executing <a class="header-anchor" href="#executing" aria-label="Permalink to &quot;Executing {#executing}&quot;">​</a></h2><p>A grouped query answers rows, not records, so every adapter has a separate entry point. The record entry points (<code>execute</code>, <code>compileQuery</code>, <code>applyQuery</code>, prisma <code>findMany</code> / <code>count</code>) refuse a grouped query with <code>AdapterError</code> (<code>FEATURE_UNSUPPORTED</code>, feature <code>groups</code>).</p><table tabindex="0"><thead><tr><th>Adapter</th><th>Entry point</th><th>Buckets</th></tr></thead><tbody><tr><td><a href="/packages/adapter-memory.html#grouped-queries">@rapiq/adapter-memory</a></td><td><code>applyGroupedQuery(query, data)</code> / <code>compileGroupedQuery(query)</code></td><td>every unit</td></tr><tr><td><a href="/packages/adapter-sql.html#grouped-queries">@rapiq/adapter-sql</a></td><td><code>adapter.executeGrouped(query)</code> + <code>normalizeGroupedRows(query, rows)</code></td><td><code>pg</code>, <code>mysql</code>, <code>sqlite</code>; <code>mssql</code> and <code>oracle</code> refuse (<code>groups:bucket</code>)</td></tr><tr><td><a href="/packages/adapter-typeorm.html#grouped-queries">@rapiq/adapter-typeorm</a></td><td><code>adapter.executeGrouped(query)</code> then <code>normalize(await qb.getRawMany())</code></td><td>PostgreSQL, MySQL, SQLite</td></tr><tr><td><a href="/packages/adapter-prisma.html#limitations">@rapiq/adapter-prisma</a></td><td>none: refused (<code>groups</code>)</td><td></td></tr><tr><td><a href="/packages/adapter-drizzle.html#limitations">@rapiq/adapter-drizzle</a></td><td>none: refused (<code>groups</code>)</td><td></td></tr></tbody></table><p>TypeORM, the common case:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { TypeormAdapter } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-typeorm&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> queryBuilder</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dataSource.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getRepository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Event).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createQueryBuilder</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;event&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">normalize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> TypeormAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ queryBuilder }).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">executeGrouped</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rows</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> normalize</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> queryBuilder.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getRawMany</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [{ bucket: &#39;2026-09-22T00:00:00.000Z&#39;, scope: &#39;auth&#39;, name: &#39;login&#39;, count: 3 }, ...]</span></span></code></pre></div><p>Build a fresh builder per call. <code>executeGrouped</code> rewrites the builder in place, and a second call on the same builder finds the first call&#39;s <code>GROUP BY</code> and is refused (<code>groups:builder</code>).</p><p>In memory, for tests and guards that must agree with the database:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { applyGroupedQuery } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-memory&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">total</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pagination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> applyGroupedQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, events);</span></span></code></pre></div><div class="warning custom-block"><p class="custom-block-title">Filters across a to-many relation</p><p>A filter on a to-many relation path (<code>filter[items.name]=...</code>) joins that relation, and a join repeats each root row per related row, which inflates <code>count</code> and <code>sum</code>. <code>@rapiq/adapter-typeorm</code> knows the cardinality and refuses aggregates over any to-many join (<code>aggregates:fan-out</code>). Standalone <code>@rapiq/adapter-sql</code> has no relation metadata and cannot tell: it renders the join, and the caller owns it (render it as a semi-join, <code>EXISTS</code>, or do not allow to-many filter paths on a grouped endpoint).</p></div><h2 id="zero-fill" tabindex="-1">Zero-filling <a class="header-anchor" href="#zero-fill" aria-label="Permalink to &quot;Zero-filling {#zero-fill}&quot;">​</a></h2><p>A bucket without matching rows is absent from the answer. Because every bucket key is an ISO instant, the client fills the gaps from a plain <code>Date</code> loop:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> byKey</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(rows.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">row</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [row.bucket, row]));</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> series</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [];</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> from.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getTime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> to.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getTime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 24</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 60</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 60</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> bucket</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Date</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(t).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">toISOString</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    series.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">push</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(byKey.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(bucket) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { bucket, count: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>(<code>from</code> must itself be a UTC day start. Month buckets need a calendar step, <code>setUTCMonth</code>, not a fixed width.)</p><h2 id="describing" tabindex="-1">Describing <a class="header-anchor" href="#describing" aria-label="Permalink to &quot;Describing {#describing}&quot;">​</a></h2><p><code>schema.describe()</code> carries both parameters, so a discovery endpoint states the grouping vocabulary. Only <strong>open</strong> slots are listed; a fixed slot is server policy and stays hidden:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;groups&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;allowed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;status&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;period&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;fn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bucket&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;params&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;unit&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;values&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;hour&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;day&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;optional&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;aggregates&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;functions&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;count&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;fn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;count&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;params&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;field&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;values&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;couponId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;optional&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }] },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;total&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;fn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sum&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;params&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;field&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;values&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;amount&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;fee&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;optional&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }] },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;revenue&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;fn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;sum&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;params&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [] }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>null</code> means never declared, which for these two parameters means <strong>nothing is permitted</strong> (<code>&quot;groups&quot;: { &quot;allowed&quot;: null, &quot;functions&quot;: null }</code>). See <a href="/guide/schemas.html#describing-a-schema">Describing a schema</a>.</p><h2 id="errors" tabindex="-1">Errors <a class="header-anchor" href="#errors" aria-label="Permalink to &quot;Errors {#errors}&quot;">​</a></h2><p>Every rejection below fails the parse, whatever <code>throwOnFailure</code> says: a dropped group would silently change the grain of every row. The raised error is the general <code>ParseError</code> from a whole-query parse, or <code>GroupsParseError</code> / <code>AggregatesParseError</code> from a standalone sub-parser, with code <code>INPUT_REJECTED</code> and each rejection on the <a href="/guide/errors.html#issue-traces">issue trace</a>.</p><table tabindex="0"><thead><tr><th>Code</th><th>Trigger</th></tr></thead><tbody><tr><td><code>INPUT_INVALID</code></td><td>the parameter value is neither a string nor a list of strings</td></tr><tr><td><code>SYNTAX_INVALID</code></td><td>a <a href="#on-the-wire">grammar</a> violation</td></tr><tr><td><code>KEY_PATH_NOT_ALLOWED</code></td><td>a dotted identifier (relation path)</td></tr><tr><td><code>KEY_INVALID</code></td><td>an identifier that is not a valid property name</td></tr><tr><td><code>KEY_NOT_ALLOWED</code></td><td>a column or function the schema does not declare, a column outside an open slot, or a bound schema without the block</td></tr><tr><td><code>KEY_VALUE_INVALID</code></td><td>wrong number of arguments, or a unit outside the permitted units</td></tr><tr><td><code>OPERATOR_UNSUPPORTED</code></td><td>schemaless parse of a name that is neither a built-in nor a bare column</td></tr><tr><td><code>KEY_AMBIGUOUS</code></td><td>two terms with the same <a href="#output-keys">output key</a></td></tr><tr><td><code>FEATURE_UNSUPPORTED</code></td><td>a client <code>fields</code> input in a grouped query, or an opted-in parameter on a custom dialect without a groups / aggregates sub-parser</td></tr></tbody></table><p>Adapters raise <code>AdapterError</code> (<code>FEATURE_UNSUPPORTED</code>) with these <code>error.feature</code> tags:</p><table tabindex="0"><thead><tr><th>Feature</th><th>Meaning</th></tr></thead><tbody><tr><td><code>groups</code></td><td>a grouped query reached a record entry point, or prisma / drizzle</td></tr><tr><td><code>groups:empty</code></td><td>a grouped entry point received a query without groups or aggregates</td></tr><tr><td><code>fields:grouped</code></td><td>a hand-built grouped query carries fields</td></tr><tr><td><code>sorts:grouped</code></td><td>a hand-built grouped query sorts by a name that is not an <a href="#output-keys">output key</a></td></tr><tr><td><code>groups:unresolved</code> / <code>aggregates:unresolved</code></td><td>a client-built named call that no schema resolved</td></tr><tr><td><code>groups:&lt;fn&gt;</code> / <code>aggregates:&lt;fn&gt;</code></td><td>a function the adapter does not implement</td></tr><tr><td><code>groups:bucket</code></td><td>the SQL dialect has no bucket spelling (<code>mssql</code>, <code>oracle</code>)</td></tr><tr><td><code>groups:bucket-type</code></td><td>the bucketed column is not temporal (TypeORM metadata, or a <code>temporalKind</code> override returning <code>undefined</code>)</td></tr><tr><td><code>groups:builder</code></td><td>the TypeORM builder already carries a <code>GROUP BY</code></td></tr><tr><td><code>aggregates:fan-out</code></td><td>aggregates over a joined to-many relation would count join rows (TypeORM)</td></tr></tbody></table><p>A hand-built <code>Group</code> whose bucket unit is not <code>hour</code>, <code>day</code> or <code>month</code> is refused with <code>AdapterError</code> code <code>KEY_VALUE_INVALID</code> by every adapter: the unit is inlined into SQL, so nothing outside the list may pass.</p>`,78)])])}const g=e(n,[["render",o]]);export{k as __pageData,g as default};
