import{_ as i,o as s,c as t,a2 as a}from"./chunks/framework.CpXckY1Z.js";const k=JSON.parse('{"title":"Merging & Composition","description":"","frontmatter":{},"headers":[],"relativePath":"guide/merging-queries.md","filePath":"guide/merging-queries.md","lastUpdated":1785870115000}'),n={name:"guide/merging-queries.md"};function r(o,e,l,d,h,p){return s(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="merging-composition" tabindex="-1">Merging &amp; Composition <a class="header-anchor" href="#merging-composition" aria-label="Permalink to &quot;Merging &amp; Composition&quot;">​</a></h1><p>Real queries rarely come from one place. A list view combines <strong>user input</strong> (a search box), <strong>component state</strong> (the current page), <strong>props</strong> (a parent-imposed scope) and <strong>application defaults</strong>, and the server may add conditions of its own. rapiq composes all of these on the <code>Query</code> itself, so every construction path (built, decoded, parsed by any dialect) combines the same way.</p><h2 id="mergequeries-left-priority" tabindex="-1"><code>mergeQueries</code>: left priority <a class="header-anchor" href="#mergequeries-left-priority" aria-label="Permalink to &quot;\`mergeQueries\`: left priority&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { mergeQueries } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> query</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> mergeQueries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(searchQuery, paginationQuery, propsQuery, defaultsQuery);</span></span></code></pre></div><p>The first argument wins conflicts. All operations are <strong>immutable</strong>: inputs stay untouched and new instances are returned, safe for reactivity systems and shared default fragments.</p><p>Per-parameter semantics:</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Rule</th></tr></thead><tbody><tr><td><code>fields</code> / <code>relations</code> / <code>sorts</code></td><td>keyed by name, left-priority replace; order = first occurrence</td></tr><tr><td><code>pagination</code></td><td>per-property left priority (<code>limit</code> and <code>offset</code> merge independently)</td></tr><tr><td><code>filters</code></td><td>per-field replace via <code>Filters.merge</code> (see below)</td></tr></tbody></table><p>One guard applies to <code>fields</code>: a name collision that would discard a <a href="/guide/fields.html#row-scoped-fields">row-scoped visibility gate</a> (<code>Field.condition</code>) throws a <code>MergeError</code> (<code>ErrorCode.FIELDS_CONDITION_DISCARDED</code>) instead of silently un-gating the column. A gate only ever arises server-side, from a schema hook; keep that parsed query as the receiver and its gates survive every collision.</p><h2 id="filters-two-explicit-operations" tabindex="-1">Filters: two explicit operations <a class="header-anchor" href="#filters-two-explicit-operations" aria-label="Permalink to &quot;Filters: two explicit operations&quot;">​</a></h2><p>Filters get two distinct operations, because a single &quot;merge&quot; would silently break one of the two use cases.</p><h3 id="merge-per-field-replace" tabindex="-1"><code>merge()</code>: per-field replace <a class="header-anchor" href="#merge-per-field-replace" aria-label="Permalink to &quot;\`merge()\`: per-field replace&quot;">​</a></h3><p>A condition on a field <strong>replaces</strong> the other side&#39;s conditions on the same field (it is <em>not</em> and-ed). This is the list-view case: user search input overrides a default on the same field, while unrelated defaults survive:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> searchQ</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({ filters: { name: { $contains: input } } });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> defaultsQ</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({ filters: { name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;John&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, age: { $gte: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mergeQueries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(searchQ, defaultsQ).filters;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// name contains &lt;input&gt; (from searchQ), age &gt;= 18 (from defaultsQ)</span></span></code></pre></div><p>Replacement applies to the <strong>displaceable root conditions</strong> of both sides: the leaf conditions directly under a root <code>and</code>. Everything else is inert, carried through as it is and combined with <code>and</code>:</p><table tabindex="0"><thead><tr><th>Conjunct</th><th>Behavior in a merge</th></tr></thead><tbody><tr><td>root-level leaf condition</td><td>replaced by a receiver condition on the same field</td></tr><tr><td><a href="#seal-conditions-that-resist-replacement">sealed</a> condition</td><td>never replaced, always carried through</td></tr><tr><td>nested group (<code>or(...)</code>, <code>not(...)</code>, a whole non-<code>and</code> root)</td><td>never replaced, always carried through as one conjunct</td></tr></tbody></table><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mergeQueries</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(flatQ, </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineQuery</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ filters: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">or</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">gte</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;age&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">18</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">eq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;email&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)) })).filters;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// and( &lt;flatQ conditions&gt;, or(age &gt;= 18, email = null) )</span></span></code></pre></div><p>Per-field replace has no sound reading inside a disjunction: dropping a condition there would change the group non-locally, keeping it would ignore receiver priority. So the group is and-ed in untouched, which makes <code>merge()</code> <strong>total</strong>: it never throws. Every conjunct of the receiver survives into the result, so a merge never returns anything wider than its receiver; only the other side can lose conditions, and only to same-field replacement. Widening stays the explicit operation, <code>or()</code> below.</p><p>An empty side passes the other side through unchanged.</p><h3 id="seal-conditions-that-resist-replacement" tabindex="-1"><code>seal</code>: conditions that resist replacement <a class="header-anchor" href="#seal-conditions-that-resist-replacement" aria-label="Permalink to &quot;\`seal\`: conditions that resist replacement&quot;">​</a></h3><p>A <strong>sealed</strong> condition is never displaced by a merge and never collapsed into its parent group by <code>flatten()</code>. It is what <code>and()</code> / <code>or()</code> apply to everything they inject, and it is available on its own:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { eq, seal } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> scope</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> seal</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">eq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;realm_id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, actor.realmId));</span></span></code></pre></div><p>Sealing is immutable (a sealed copy is returned) and idempotent. It is a <strong>server-side composition marker, not wire grammar</strong>: a sealed condition that is encoded to a URL and decoded again comes back displaceable, which is why a receiving service re-injects its own scope instead of trusting the transport.</p><p>The marker protects the whole subtree it heads, and it holds during parsing too: the <a href="/guide/relations.html#validate-hooks">relations gate</a>, which drops every key traversing a rejected relation, cannot silently prune a condition out of a sealed group. A rejected relation that a sealed condition needs throws <code>SchemaError</code> (<code>ErrorCode.SCHEMA_SEALED_CONDITION_PRUNED</code>) instead. Subtree-wide protection is why a <a href="/guide/recipes/authorization.html#scoping-inject-conditions-the-client-cannot-displace">policy residual</a> seals the <em>scope</em> rather than the group around it: sealing the group would protect the client&#39;s own condition too.</p><h3 id="and-or-wrap-inject" tabindex="-1"><code>and()</code> / <code>or()</code>: wrap &amp; inject <a class="header-anchor" href="#and-or-wrap-inject" aria-label="Permalink to &quot;\`and()\` / \`or()\`: wrap &amp; inject&quot;">​</a></h3><p>Always defined, for combining condition trees. The injected conditions are sealed, so they are part of the tree rather than candidates for replacement. A receiver already carrying that operator contributes its own conditions to the group (which keeps them mergeable); anything else becomes a child of the new group. Calling <code>and()</code> / <code>or()</code> with no conditions returns the receiver unchanged.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Query, eq } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// server-side, after parsing client input:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> scoped</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    ...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">query,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: query.filters.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">and</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">eq</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;realm_id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, actor.realmId)),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The adapter output now contains the injected condition regardless of what the client sent, even when the client sent no filters at all. And because the injected condition is sealed, no later composition can drop it: a <code>merge()</code> carries it through and a client condition on the same field narrows within it, while <code>flatten()</code> and any other normalization leave the marker intact. That guarantee is unconditional, and it is a feature: injected security conditions cannot be merged away. See the <a href="/guide/recipes/authorization.html">authorization recipe</a>.</p><h2 id="why-not-deep-merge-input-objects" tabindex="-1">Why not deep-merge input objects? <a class="header-anchor" href="#why-not-deep-merge-input-objects" aria-label="Permalink to &quot;Why not deep-merge input objects?&quot;">​</a></h2><p>Generic object merging (smob, deepmerge, spread) on query <em>input</em> cannot express these semantics: same-field replace, null-preserving <code>in</code> arrays, injected conditions that resist displacement. Merge on the query instead: build fragments with <a href="/guide/building-queries.html#fragments"><code>defineQuery</code> / <code>define*</code></a>, compose with <code>mergeQueries</code>, then encode or execute the result.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><a href="/guide/recipes/frontend.html">Recipes: Type-safe frontend queries</a>: merging user input, props and defaults in a list view.</li><li><a href="/guide/recipes/authorization.html">Recipes: Authorization &amp; scoping</a>: <code>and()</code> injection end-to-end.</li></ul>`,31)])])}const g=i(n,[["render",r]]);export{k as __pageData,g as default};
