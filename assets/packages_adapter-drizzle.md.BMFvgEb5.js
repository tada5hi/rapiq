import{_ as s,o as i,c as a,a2 as t}from"./chunks/framework.CpXckY1Z.js";const k=JSON.parse('{"title":"@rapiq/adapter-drizzle","description":"","frontmatter":{},"headers":[],"relativePath":"packages/adapter-drizzle.md","filePath":"packages/adapter-drizzle.md","lastUpdated":1786368951000}'),n={name:"packages/adapter-drizzle.md"};function l(r,e,d,h,p,o){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="rapiq-adapter-drizzle" tabindex="-1">@rapiq/adapter-drizzle <a class="header-anchor" href="#rapiq-adapter-drizzle" aria-label="Permalink to &quot;@rapiq/adapter-drizzle&quot;">​</a></h1><p>Serializes a parsed <a href="/guide/query-ast.html"><code>Query</code></a> into a <strong>Drizzle relational query config</strong>: filters become <code>where</code>, fields become <code>columns</code>, relations become <code>with</code>, sort becomes <code>orderBy</code> and pagination becomes <code>limit</code>/<code>offset</code>.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/core</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @rapiq/adapter-drizzle</span></span></code></pre></div><p>The adapter targets the object-based relational queries v2 API of <strong>drizzle-orm v1</strong>. Like the Prisma adapter there is nothing to mutate: it is a pure serializer that returns a plain object. <code>drizzle-orm</code> is neither a runtime nor a peer dependency of this package; it is a devDependency only, because the test suite runs the emitted configs through a real engine.</p><h2 id="usage" tabindex="-1">Usage <a class="header-anchor" href="#usage" aria-label="Permalink to &quot;Usage&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { DrizzleAdapter, defineMetadata } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@rapiq/adapter-drizzle&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DrizzleAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    provider: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pg&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    metadata: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineMetadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(datamodel, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;users&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">pagination</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> users</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db.query.users.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">findMany</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(config);</span></span></code></pre></div><p><code>provider</code> names the dialect the config will execute against (see <a href="#case-sensitivity">Case sensitivity</a>); <code>metadata</code> supplies the table facts the serializer needs (see <a href="#table-metadata">Table metadata</a>). <code>pagination</code> echoes the limit/offset actually applied, e.g. for a response <code>meta</code> block.</p><p>Because the adapter produces a value instead of writing into a builder, it is <strong>stateless</strong>: construct one instance per table and share it across requests. To combine several queries, compose them <em>before</em> serializing with <a href="/guide/merging-queries.html"><code>mergeQueries</code></a> or <code>query.filters.and(...)</code>; the adapter deliberately has no accumulation API. <code>buildDrizzleConfig(query, options)</code> is the one-shot convenience form.</p><h2 id="table-metadata" tabindex="-1">Table metadata <a class="header-anchor" href="#table-metadata" aria-label="Permalink to &quot;Table metadata&quot;">​</a></h2><p>The adapter needs four facts about your tables that a <code>Query</code> cannot carry, and each one changes what a <em>correct</em> drizzle filter looks like:</p><table tabindex="0"><thead><tr><th>fact</th><th>what it decides</th></tr></thead><tbody><tr><td>is the path a relation?</td><td>a relation takes a nested filter object or a presence test, never a column operator</td></tr><tr><td>is that relation to-many?</td><td>the shape of the empty-collection arm of a complement</td></tr><tr><td>can the column hold <code>null</code>?</td><td>whether a complement carries its <code>isNull</code> arm</td></tr><tr><td>does it hold strings?</td><td>only string columns fold case through <code>ilike</code></td></tr></tbody></table><p>Guessing any of them produces a wrong result set rather than graceful degradation, so the adapter refuses to run without them. The datamodel is a plain object in drizzle&#39;s own vocabulary (<code>dataType</code> as on a drizzle column, relations keyed the way <code>defineRelations</code> keys them); a bare string is shorthand for <code>{ dataType }</code>:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> metadata</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> defineMetadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    users: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        columns: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            id: { dataType: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;number&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, nullable: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;string&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            address: { dataType: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;string&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, nullable: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        relations: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            items: { target: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;items&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, many: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            realm: { target: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;realms&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, many: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    items: { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    realms: { </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* ... */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;users&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>An undeclared fact is treated as unknown, never guessed: an unknown nullability keeps the complement&#39;s null arm (semantically safe either way), an unknown data type keeps the case fold.</p><h2 id="parameter-mapping" tabindex="-1">Parameter mapping <a class="header-anchor" href="#parameter-mapping" aria-label="Permalink to &quot;Parameter mapping&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Query parameter</th><th>findMany config key</th></tr></thead><tbody><tr><td><code>filters</code></td><td><code>where</code></td></tr><tr><td><code>fields</code></td><td><code>columns</code></td></tr><tr><td><code>relations</code></td><td><code>with</code> (nested <code>columns</code> when fields are picked)</td></tr><tr><td><code>sort</code></td><td><code>orderBy</code> (one object, key order carries the priority)</td></tr><tr><td><code>pagination</code></td><td><code>limit</code> / <code>offset</code></td></tr></tbody></table><p>Unlike Prisma&#39;s <code>select</code>/<code>include</code>, drizzle keeps scalar selection (<code>columns</code>) and relation hydration (<code>with</code>) in separate keys that compose at every level. An explicitly included relation without direct picks is hydrated whole; a per-relation fieldset (direct <code>relation.field</code> picks) narrows it to exactly those columns, matching the projection contract of <a href="/packages/adapter-memory.html">@rapiq/adapter-memory</a> (<a href="https://github.com/tada5hi/rapiq/issues/847" target="_blank" rel="noreferrer">#847</a>). Picks belonging to a deeper relation never narrow the traversed prefix, and a level reached only to project deeper emits <code>columns: {}</code> (no scalars of its own).</p><p>Every column a <a href="/guide/schemas.html#condition-verdicts">field visibility gate</a> reads is force-projected into <code>columns</code> (the gates themselves are enforced post-fetch via <code>@rapiq/adapter-memory</code>&#39;s <code>applyFieldConditions</code>, exactly as with the SQL backends). Those operand entries never narrow an include: behind a pick-free whole relation the hydration covers them anyway.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fields: [&#39;id&#39;, &#39;realm.name&#39;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">columns</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { realm: { columns: { name: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } } }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// fields: [&#39;id&#39;], relations: [&#39;realm&#39;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">columns</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">id</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { realm: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// relations: [&#39;items&#39;, &#39;items.realm&#39;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">with</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { items: { with: { realm: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } } }</span></span></code></pre></div><p>An unsatisfiable condition (e.g. <code>in([])</code>) has no dialect-free false literal in the filter object (drizzle strips an empty <code>OR</code> group instead of failing the row set), so it is expressed through the config: <code>limit: 0</code> returns no rows on every dialect, and a caller-owned baseline cannot widen it.</p><h2 id="preserving-an-application-owned-predicate" tabindex="-1">Preserving an application-owned predicate <a class="header-anchor" href="#preserving-an-application-owned-predicate" aria-label="Permalink to &quot;Preserving an application-owned predicate&quot;">​</a></h2><p>Rapiq filters <strong>narrow</strong> a query, they never replace it. Hand the adapter a baseline config and the client&#39;s conditions are conjoined with your tenant or authorization scope:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    base: { where: { realm_id: realmId } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// where: { AND: [ { realm_id: ... }, { ...client filters } ] }</span></span></code></pre></div><p>An overriding <code>columns</code> replaces the baseline projection (a caller-owned projection is never widened), produced <code>with</code> entries join baseline ones, and <code>orderBy</code>/<code>limit</code>/<code>offset</code> follow the override. The merge rules are exported as <code>mergeConfig(base, override)</code> for use outside the adapter.</p><h2 id="negation" tabindex="-1">Negation <a class="header-anchor" href="#negation" aria-label="Permalink to &quot;Negation&quot;">​</a></h2><p>rapiq&#39;s negated operators (<code>ne</code>, <code>nin</code>, <code>notContains</code>, ...) and <code>not(...)</code> groups are the <strong>exact null-inclusive complement</strong> of their positive twin: they match precisely the records the positive form does not. Drizzle renders <code>NOT</code> as plain SQL <code>NOT (...)</code>, which follows three-valued logic and silently drops rows whose column is <code>NULL</code>.</p><p>The adapter therefore never applies drizzle&#39;s <code>NOT</code> to a user condition. It pushes negation down to the leaves (De Morgan, via <code>@rapiq/core</code>&#39;s <code>distributeNegation</code>) and renders each complement itself; <code>isNull</code>/<code>isNotNull</code> make every arm expressible in the filter object:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ne(&#39;address&#39;, &#39;Mordor&#39;)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">OR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [ { address: { notIlike: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Mordor&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } }, { address: { isNull: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } ] }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ne(&#39;realm.name&#39;, &#39;master&#39;): realm is an optional to-one relation</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">OR</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { realm: { name: { notIlike: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;master&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { NOT: { realm: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ...or there is no realm at all</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] }</span></span></code></pre></div><p>The one emitted <code>NOT</code> negates a relation <strong>presence test</strong> (<code>{ realm: true }</code>), which compiles to <code>EXISTS</code> and is two-valued by construction. When metadata proves a column non-nullable the null arm is dropped, because no null can exist there. The result set is identical to <code>@rapiq/adapter-sql</code>, <code>@rapiq/adapter-typeorm</code>, <code>@rapiq/adapter-prisma</code> and <code>@rapiq/adapter-memory</code>; the complement law is pinned by the engine-backed parity suite.</p><p><strong>Same-element binding holds.</strong> A relation filter object is a single correlated <code>EXISTS</code> scope in drizzle, so conditions sharing a to-many path are factored into ONE object and bind to the same related record, exactly like the per-join-row evaluation of the SQL backends:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// and(eq(&#39;items.title&#39;, &#39;book&#39;), eq(&#39;items.color&#39;, &#39;red&#39;))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">AND</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { title: { ilike: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;book&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { color: { ilike: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;red&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] } }</span></span></code></pre></div><p>A record whose elements satisfy the two conditions only separately does not match, on any backend. Trees mixing root and relation conditions are expanded distributively first (bounded; a tree beyond the bound fails typed instead of degrading), and every quantifier gains a <code>NOT: { relation: true }</code> empty-collection arm exactly when its interior holds at the all-null binding an empty collection contributes. <a href="/guide/filters.html#elemmatch"><code>elemMatch</code></a> remains the explicit same-element construct and maps onto the same single scope.</p><h2 id="case-sensitivity" tabindex="-1">Case sensitivity <a class="header-anchor" href="#case-sensitivity" aria-label="Permalink to &quot;Case sensitivity&quot;">​</a></h2><p>String comparison is <a href="/guide/filters.html#case-sensitivity">case-insensitive by default</a> across the rapiq backends. Drizzle has no <code>mode: &#39;insensitive&#39;</code>; the fold is expressed through operators, so the adapter takes a provider, and dialect capability decides how far the contract reaches (on sqlite the equality family cannot fold, see the table):</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DrizzleAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ provider: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;mysql&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, metadata });</span></span></code></pre></div><table tabindex="0"><thead><tr><th>provider</th><th>behavior</th></tr></thead><tbody><tr><td><code>pg</code></td><td>anchored operators use <code>ilike</code>; an insensitive equality/membership lowers to <code>ilike</code> with a fully escaped operand</td></tr><tr><td><code>mysql</code></td><td>plain operators: the default <code>*_ci</code> collation already compares case-insensitively</td></tr><tr><td><code>sqlite</code></td><td><code>like</code> is ASCII-case-insensitive, <code>eq</code>/<code>in</code> are <strong>not</strong>: a documented limitation</td></tr></tbody></table><p><code>provider</code> is required, and an unrecognized name throws rather than falling back: emitting <code>ilike</code> on a dialect without it breaks every case-insensitive filter, which is too sharp an edge to guess at.</p><p>Because the adapter builds the LIKE operand itself, a lowered equality escapes <code>%</code>, <code>_</code> and <code>\\</code> first: <code>eq(&#39;code&#39;, &#39;50%&#39;)</code> matches exactly the literal string, never a wildcard pattern. (The Prisma adapter needs a wildcard veto here because Prisma passes the operand through verbatim; this adapter does not.) An insensitive membership decomposes into <code>ilike</code> arms per string member, keeping non-string members an exact <code>in</code>.</p><p>Two boundaries remain. A non-string column never folds. And on dialects without a default LIKE escape character (sqlite), an anchored operator whose text contains a literal <code>%</code> or <code>_</code> cannot be matched exactly: the adapter fails typed instead of silently widening the match.</p><p>Opt individual fields out through the schema:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">defineSchema</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">User</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    filters: { allowed: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;id&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;token&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], caseSensitive: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;token&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>The declaration is not consumed automatically: the receiving side forwards it to the adapter, mirroring the <a href="/guide/filters.html#case-sensitivity">shared contract</a>. The adapter takes <code>caseSensitive</code> as a top-level constructor option (a field list, or <code>true</code> to opt every field out), and <code>execute</code> accepts it as a per-call override:</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> adapter</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DrizzleAdapter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    provider: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;pg&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    metadata,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    caseSensitive: schema.filters.caseSensitive,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// per call, overriding the constructor value:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">adapter.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(query, { caseSensitive: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre></div><h2 id="limitations" tabindex="-1">Limitations <a class="header-anchor" href="#limitations" aria-label="Permalink to &quot;Limitations&quot;">​</a></h2><p>Operators without a drizzle filter equivalent raise a typed <code>AdapterError</code> (<code>FEATURE_UNSUPPORTED</code>) instead of being approximated:</p><ul><li><code>regex</code>: the filter object exposes no regular-expression operator</li><li><code>mod</code>: no modulo filter</li><li><code>size</code>: no array-length filter</li><li><code>elemMatch</code> on a scalar array and the <code>$this</code> element marker: only to-many <em>relations</em> have addressable elements</li><li><strong>ordering by a relation path</strong>: the relational API orders the root by its own columns only; an undocumented nested shape could be silently ignored, and loud beats silent</li></ul><p><code>exists()</code> on a to-many relation is constantly true: it asks whether a <em>value is present</em>, and a collection is possibly empty, never absent. That matches <a href="/packages/adapter-memory.html">@rapiq/adapter-memory</a>.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>The default test suite already executes every emitted config through a real drizzle engine: in-memory SQLite via <code>better-sqlite3</code>, no codegen, no server. <code>npm run test:db</code> replays the same parity matrix against PostgreSQL when <code>DB_TYPE=postgres</code> names a live server (CI runs it), which is the only place <code>ilike</code> exists and the case contract can be measured. Every condition is cross-checked against <a href="/packages/adapter-memory.html">@rapiq/adapter-memory</a>; the dialect-specific claims on this page are measurements from that suite rather than assumptions.</p><h2 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-label="Permalink to &quot;Documentation&quot;">​</a></h2><ul><li><a href="/guide/filters.html">Filters</a>: operator reference and case semantics</li><li><a href="/guide/executing-queries.html">Executing Queries</a>: the adapter surface shared by all backends</li></ul>`,51)])])}const E=s(n,[["render",l]]);export{k as __pageData,E as default};
